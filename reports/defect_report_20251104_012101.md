# 🔍 代码缺陷检测与修复报告

**生成时间**: 2025-11-04 01:21:01

---

## 📊 执行摘要

### 🎯 总体情况

- **静态分析（内置规则）**: 1239 个
  - 🔴 高危: 555 个
  - 🟡 中危: 485 个
  - 🟢 低危: 199 个
- **外部工具检测**: 847 个
- **动态检测（编译错误）**: 0 个
- **总计问题**: 2086 个

- **成功修复**: 0 个问题
- **修复文件数**: 26 个

- **验证通过**: 0 个文件
- **验证失败**: 0 个文件

## 🔧 外部工具执行情况

| 工具 | 检测数量 | 状态 |
|------|---------|------|
| ruff | 285 | ✅ 正常 |
| pylint | 440 | ✅ 正常 |
| mypy | 1 | ✅ 正常 |
| bandit | 121 | ✅ 正常 |

## 🐛 缺陷详情与修复方案

### 🔴 HIGH 级别 (555 个)

#### 📄 `__init__.py`

**1. [PY100] 第 137 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_C'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_C,
  ```

**2. [PY100] 第 138 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_GO'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_GO,
  ```

**3. [PY100] 第 139 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_JAVA'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_JAVA,
  ```

**4. [PY100] 第 140 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_JS'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_JS,
  ```

**5. [PY100] 第 141 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_PHP'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_PHP,
  ```

**6. [PY100] 第 142 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_PY'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_PY,
  ```

**7. [PY100] 第 143 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_RUBY'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_RUBY,
  ```

**8. [PY100] 第 144 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_RUST'。
- **原代码**:
  ```python
  **MAP_REPO_VERSION_TO_SPECS_RUST,
  ```

**9. [PY100] 第 148 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_C'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_C,
  ```

**10. [PY100] 第 149 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_GO'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_GO,
  ```

**11. [PY100] 第 150 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_JAVA'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_JAVA,
  ```

**12. [PY100] 第 151 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_JS'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_JS,
  ```

**13. [PY100] 第 152 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_PHP'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_PHP,
  ```

**14. [PY100] 第 153 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_PY'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_PY,
  ```

**15. [PY100] 第 154 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_RUBY'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_RUBY,
  ```

**16. [PY100] 第 155 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_TO_INSTALL_RUST'。
- **原代码**:
  ```python
  **MAP_REPO_TO_INSTALL_RUST,
  ```

**17. [PY100] 第 159 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_C'。
- **原代码**:
  ```python
  **{k: "c" for k in MAP_REPO_VERSION_TO_SPECS_C.keys()},
  ```

**18. [PY100] 第 160 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_GO'。
- **原代码**:
  ```python
  **{k: "go" for k in MAP_REPO_VERSION_TO_SPECS_GO.keys()},
  ```

**19. [PY100] 第 161 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_JAVA'。
- **原代码**:
  ```python
  **{k: "java" for k in MAP_REPO_VERSION_TO_SPECS_JAVA.keys()},
  ```

**20. [PY100] 第 162 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_JS'。
- **原代码**:
  ```python
  **{k: "js" for k in MAP_REPO_VERSION_TO_SPECS_JS.keys()},
  ```

**21. [PY100] 第 163 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_PHP'。
- **原代码**:
  ```python
  **{k: "php" for k in MAP_REPO_VERSION_TO_SPECS_PHP.keys()},
  ```

**22. [PY100] 第 164 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_PY'。
- **原代码**:
  ```python
  **{k: "py" for k in MAP_REPO_VERSION_TO_SPECS_PY.keys()},
  ```

**23. [PY100] 第 165 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_RUBY'。
- **原代码**:
  ```python
  **{k: "rb" for k in MAP_REPO_VERSION_TO_SPECS_RUBY.keys()},
  ```

**24. [PY100] 第 166 行**
- **问题**: 使用了未定义的名称 'MAP_REPO_VERSION_TO_SPECS_RUST'。
- **原代码**:
  ```python
  **{k: "rs" for k in MAP_REPO_VERSION_TO_SPECS_RUST.keys()},
  ```

**25. [PY100] 第 170 行**
- **问题**: 使用了未定义的名称 'USE_X86_PY'。
- **原代码**:
  ```python
  USE_X86 = USE_X86_PY
  ```

#### 📄 `bm25_retrieval.py`

**1. [PY100] 第 66 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = list(filter(lambda x: os.path.isfile(x), files))
  ```

**2. [PY100] 第 67 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = list(filter(lambda x: x.lower().startswith("readme"), files))
  ```

**3. [PY100] 第 261 行**
- **问题**: 使用了未定义的名称 'KeyboardInterrupt'。
- **原代码**:
  ```python
  except KeyboardInterrupt:
  ```

**4. [PY100] 第 263 行**
- **问题**: 使用了未定义的名称 'KeyboardInterrupt'。
- **原代码**:
  ```python
  raise KeyboardInterrupt
  ```

**5. [PY100] 第 266 行**
- **问题**: 使用了未定义的名称 'KeyboardInterrupt'。
- **原代码**:
  ```python
  raise KeyboardInterrupt
  ```

**6. [PY100] 第 334 行**
- **问题**: 使用了未定义的名称 'round'。
- **原代码**:
  ```python
  cutoff = int(round(cutoff * 0.8))
  ```

**7. [PY003] 第 486 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  python = subprocess.run("which python", shell=True, capture_output=True)
  ```

**8. [PY100] 第 504 行**
- **问题**: 使用了未定义的名称 'KeyboardInterrupt'。
- **原代码**:
  ```python
  except KeyboardInterrupt:
  ```

**9. [RUFF-E501] 第 41 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  __enter__(): Switches to the specified commit and returns the context manager object.
  ```

**10. [RUFF-E501] 第 42 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  get_readme_files(): Returns a list of filenames for all README files in the repository.
  ```

**11. [RUFF-F841] 第 115 行**
- **问题**: Local variable `abspath` is assigned to but never used
- **原代码**:
  ```python
  abspath = Path(filename).absolute()
  ```

**12. [RUFF-E722] 第 136 行**
- **问题**: Do not use bare `except`
- **原代码**:
  ```python
  except:
  ```

**13. [RUFF-E501] 第 181 行**
- **问题**: Line too long (134 > 88)
- **原代码**:
  ```python
  document_encoding_func (function): A function that takes a filename and a relative path and returns the encoded document text.
  ```

**14. [RUFF-E501] 第 184 行**
- **问题**: Line too long (127 > 88)
- **原代码**:
  ```python
  dict: A dictionary where the keys are the relative paths of the documents and the values are the encoded document text.
  ```

**15. [RUFF-E501] 第 278 行**
- **问题**: Line too long (102 > 88)
- **原代码**:
  ```python
  Filters a list of instances to exclude those that have already been processed and saved in a file.
  ```

**16. [RUFF-E501] 第 281 行**
- **问题**: Line too long (115 > 88)
- **原代码**:
  ```python
  instances (List[Dict]): A list of instances, where each instance is a dictionary with an "instance_id" key.
  ```

**17. [RUFF-E501] 第 282 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  output_file (Path): The path to the file where the processed instances are saved.
  ```

**18. [RUFF-E501] 第 297 行**
- **问题**: Line too long (97 > 88)
- **原代码**:
  ```python
  f"Found {len(instance_ids)} existing instances in {output_file}. Will skip them."
  ```

**19. [RUFF-E501] 第 318 行**
- **问题**: Line too long (119 > 88)
- **原代码**:
  ```python
  dict: A dictionary containing the instance ID and a list of hits, where each hit is a dictionary containing the
  ```

**20. [RUFF-E501] 第 351 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  Searches the indexes for the given instances and writes the results to the output file.
  ```

**21. [RUFF-E501] 第 356 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  all_index_paths (dict): A dictionary mapping instance IDs to the paths of their indexes.
  ```

**22. [RUFF-E722] 第 409 行**
- **问题**: Do not use bare `except`
- **原代码**:
  ```python
  except:
  ```

**23. [RUFF-E501] 第 532 行**
- **问题**: Line too long (106 > 88)
- **原代码**:
  ```python
  help="Dataset to use for test set from HuggingFace Datasets or path to a save_to_disk directory.",
  ```

**24. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'jedi'
- **原代码**:
  ```python
  import jedi
  ```

**25. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'filelock'
- **原代码**:
  ```python
  from filelock import FileLock
  ```

**26. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import load_from_disk, load_dataset
  ```

**27. [PL-import-error] 第 11 行**
- **问题**: Unable to import 'pyserini.search.lucene'
- **原代码**:
  ```python
  from pyserini.search.lucene import LuceneSearcher
  ```

**28. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'git'
- **原代码**:
  ```python
  from git import Repo
  ```

**29. [PL-import-error] 第 17 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.utils'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.utils import list_files, string_to_bool
  ```

#### 📄 `build_dataset.py`

**1. [RUFF-E501] 第 183 行**
- **问题**: Line too long (123 > 88)
- **原代码**:
  ```python
  f"[{', '.join(repos.keys())}] Total instances: {total_instances}, completed: {completed}, with tests: {with_tests}"
  ```

**2. [RUFF-E501] 第 186 行**
- **问题**: Line too long (109 > 88)
- **原代码**:
  ```python
  f"[{', '.join(repos.keys())}] Skipped {len(seen_prs)} pull requests that have already been inspected"
  ```

**3. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'swebench.collect.utils'
- **原代码**:
  ```python
  from swebench.collect.utils import (
  ```

#### 📄 `build_dataset_ft.py`

**1. [RUFF-E501] 第 62 行**
- **问题**: Line too long (104 > 88)
- **原代码**:
  ```python
  f"Fine tuning dataset saved to {destination} ({total_insts} instances from {total_repos} repos)"
  ```

#### 📄 `c.py`

**1. [PY100] 第 83 行**
- **问题**: 使用了未定义的名称 'expr'。
- **原代码**:
  ```python
  expr.get("success") == "true" for expr in expressions
  ```

**2. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import TestStatus
  ```

**3. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

#### 📄 `call_make_repo.py`

**1. [PY003] 第 9 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  out_make = subprocess.run(
  ```

**2. [B602] 第 11 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  shell=True,
  ```

#### 📄 `create_instance.py`

**1. [PY100] 第 396 行**
- **问题**: 使用了未定义的名称 'v'。
- **原代码**:
  ```python
  k: v for k, v in instances.items() if k not in processed_ids
  ```

**2. [RUFF-E501] 第 166 行**
- **问题**: Line too long (117 > 88)
- **原代码**:
  ```python
  premise = "You will be provided with a partial code base and an issue statement explaining a problem to resolve."
  ```

**3. [RUFF-E501] 第 170 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  "I need you to solve this issue by generating a single patch file that I can apply "
  ```

**4. [RUFF-E501] 第 171 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  + "directly to this repository using git apply. Please respond with a single patch "
  ```

**5. [RUFF-E501] 第 194 行**
- **问题**: Line too long (117 > 88)
- **原代码**:
  ```python
  premise = "You will be provided with a partial code base and an issue statement explaining a problem to resolve."
  ```

**6. [RUFF-E501] 第 198 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  "I need you to solve this issue by generating a single patch file that I can apply "
  ```

**7. [RUFF-E501] 第 199 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  + "directly to this repository using git apply. Please respond with a single patch "
  ```

**8. [RUFF-E501] 第 222 行**
- **问题**: Line too long (117 > 88)
- **原代码**:
  ```python
  premise = "You will be provided with a partial code base and an issue statement explaining a problem to resolve."
  ```

**9. [RUFF-E501] 第 227 行**
- **问题**: Line too long (107 > 88)
- **原代码**:
  ```python
  + "It specifies the file names, the line numbers of each change, and the removed and added lines. "
  ```

**10. [RUFF-E501] 第 231 行**
- **问题**: Line too long (100 > 88)
- **原代码**:
  ```python
  "I need you to solve the provided issue by generating a single patch file that I can apply "
  ```

**11. [RUFF-E501] 第 232 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  + "directly to this repository using git apply. Please respond with a single patch "
  ```

**12. [RUFF-E501] 第 260 行**
- **问题**: Line too long (117 > 88)
- **原代码**:
  ```python
  premise = "You will be provided with a partial code base and an issue statement explaining a problem to resolve."
  ```

**13. [RUFF-E501] 第 264 行**
- **问题**: Line too long (120 > 88)
- **原代码**:
  ```python
  "I need you to solve this issue by regenerating the full files in the code base that you would like to change. "
  ```

**14. [RUFF-E501] 第 266 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  + "Please respond with a list of files and their revised contents in the following format."
  ```

**15. [RUFF-E501] 第 355 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  - retrieval_file: if using retrieval method for file_contents, specify retrieval_file
  ```

**16. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'unidiff'
- **原代码**:
  ```python
  import unidiff
  ```

**17. [PL-import-error] 第 11 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.tokenize_dataset'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.tokenize_dataset import TOKENIZER_FUNCS
  ```

**18. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.utils'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.utils import (
  ```

#### 📄 `create_scripts.py`

**1. [PL-import-error] 第 1 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import MAP_REPO_TO_EXT
  ```

**2. [PL-import-error] 第 2 行**
- **问题**: Unable to import 'swebench.harness.test_spec.javascript'
- **原代码**:
  ```python
  from swebench.harness.test_spec.javascript import (
  ```

**3. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'swebench.harness.test_spec.python'
- **原代码**:
  ```python
  from swebench.harness.test_spec.python import (
  ```

**4. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'swebench.harness.test_spec.utils'
- **原代码**:
  ```python
  from swebench.harness.test_spec.utils import (
  ```

#### 📄 `create_text_dataset.py`

**1. [RUFF-E721] 第 26 行**
- **问题**: Use `is` and `is not` for type comparisons, or `isinstance()` for isinstance checks
- **原代码**:
  ```python
  if type(filename) == str:
  ```

**2. [RUFF-E501] 第 147 行**
- **问题**: Line too long (105 > 88)
- **原代码**:
  ```python
  f"{output_file.absolute().as_posix()} already exists for split {split}. Aborting"
  ```

**3. [RUFF-E501] 第 223 行**
- **问题**: Line too long (180 > 88)
- **原代码**:
  ```python
  f"Found {len(invalid_instances)} instances in progress file that are not in the {split} dataset: {invalid_instances}. These will be removed from the final dataset."
  ```

**4. [RUFF-E501] 第 262 行**
- **问题**: Line too long (106 > 88)
- **原代码**:
  ```python
  help="Dataset to use for test set from HuggingFace Datasets or path to a save_to_disk directory.",
  ```

**5. [RUFF-E501] 第 313 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  help="Tokenizer to use for max_context_len. Only needed if max_context_len is specified.",
  ```

**6. [RUFF-E501] 第 318 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  help="Username to use for pushing to the Hub. If not provided, will save to disk.",
  ```

**7. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import Dataset, DatasetDict, load_dataset, load_from_disk
  ```

**8. [PL-import-error] 第 15 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.create_instance'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.create_instance import (
  ```

**9. [PL-import-error] 第 19 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.tokenize_dataset'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.tokenize_dataset import TOKENIZER_FUNCS
  ```

#### 📄 `criteria.py`

**1. [PY100] 第 68 行**
- **问题**: 使用了未定义的名称 'ext'。
- **原代码**:
  ```python
  pattern_image = "|".join(re.escape(ext) for ext in image_extensions)
  ```

**2. [RUFF-E501] 第 24 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  Returns True if the text contains a URL. Excludes URLs that are part of the repository.
  ```

**3. [RUFF-E501] 第 29 行**
- **问题**: Line too long (111 > 88)
- **原代码**:
  ```python
  # Adding a negative lookahead assertion to ensure URLs starting with the repository prefix are excluded
  ```

**4. [RUFF-E501] 第 79 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  Returns True if text (problem statement) contains a reference to another issue (e.g. #1234).
  ```

**5. [RUFF-E501] 第 117 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  Returns True if the test patch contains a pytest.raises() call with a match argument.
  ```

**6. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'swebench.collect.utils'
- **原代码**:
  ```python
  from swebench.collect.utils import PR_KEYWORDS
  ```

**7. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'unidiff'
- **原代码**:
  ```python
  from unidiff import PatchSet
  ```

#### 📄 `default.py`

**1. [PY100] 第 120 行**
- **问题**: 使用了未定义的名称 'TimeoutError'。
- **原代码**:
  ```python
  except TimeoutError:
  ```

**2. [RUFF-E501] 第 1 行**
- **问题**: Line too long (109 > 88)
- **原代码**:
  ```python
  """Basic agent class. See https://mini-swe-agent.com/latest/advanced/control_flow/ for visual explanation."""
  ```

**3. [RUFF-E501] 第 15 行**
- **问题**: Line too long (120 > 88)
- **原代码**:
  ```python
  # The default settings are the bare minimum to run the agent. Take a look at the config files for improved settings.
  ```

**4. [RUFF-E501] 第 18 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  "Your task: {{task}}. Please reply with a single shell command in triple backticks. "
  ```

**5. [RUFF-E501] 第 19 行**
- **问题**: Line too long (119 > 88)
- **原代码**:
  ```python
  "To finish, the first line of the output of the shell command must be 'COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT'."
  ```

**6. [RUFF-E501] 第 22 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  "The last command <command>{{action['action']}}</command> timed out and has been killed.\n"
  ```

**7. [RUFF-E501] 第 24 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  "Please try another command and make sure to avoid those requiring interactive input."
  ```

**8. [RUFF-E501] 第 26 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  format_error_template: str = "Please always provide EXACTLY ONE action in triple backticks."
  ```

**9. [RUFF-E501] 第 57 行**
- **问题**: Line too long (106 > 88)
- **原代码**:
  ```python
  def __init__(self, model: Model, env: Environment, *, config_class: Callable = AgentConfig, **kwargs):
  ```

**10. [RUFF-E501] 第 65 行**
- **问题**: Line too long (107 > 88)
- **原代码**:
  ```python
  template_vars = asdict(self.config) | self.env.get_template_vars() | self.model.get_template_vars()
  ```

**11. [RUFF-E501] 第 66 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  return Template(template).render(**kwargs, **template_vars, **self.extra_template_vars)
  ```

**12. [RUFF-E501] 第 92 行**
- **问题**: Line too long (109 > 88)
- **原代码**:
  ```python
  if 0 < self.config.step_limit <= self.model.n_calls or 0 < self.config.cost_limit <= self.model.cost:
  ```

**13. [RUFF-E501] 第 101 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  observation = self.render_template(self.config.action_observation_template, output=output)
  ```

**14. [RUFF-E501] 第 110 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  raise FormatError(self.render_template(self.config.format_error_template, actions=actions))
  ```

**15. [RUFF-E501] 第 118 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  self.render_template(self.config.timeout_template, action=action, output=output)
  ```

**16. [RUFF-E501] 第 121 行**
- **问题**: Line too long (117 > 88)
- **原代码**:
  ```python
  raise ExecutionTimeoutError(self.render_template(self.config.timeout_template, action=action, output=""))
  ```

**17. [RUFF-E501] 第 126 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  """Raises Submitted exception with final output if the agent has finished its task."""
  ```

**18. [RUFF-E501] 第 128 行**
- **问题**: Line too long (114 > 88)
- **原代码**:
  ```python
  if lines and lines[0].strip() in ["MINI_SWE_AGENT_FINAL_OUTPUT", "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"]:
  ```

**19. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'jinja2'
- **原代码**:
  ```python
  from jinja2 import Template
  ```

**20. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'minisweagent'
- **原代码**:
  ```python
  from minisweagent import Environment, Model
  ```

#### 📄 `distributed_attention.py`

**1. [PY100] 第 16 行**
- **问题**: 使用了未定义的名称 'staticmethod'。
- **原代码**:
  ```python
  @staticmethod
  ```

**2. [PY100] 第 34 行**
- **问题**: 使用了未定义的名称 'staticmethod'。
- **原代码**:
  ```python
  @staticmethod
  ```

**3. [PY100] 第 59 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**4. [PL-import-error] 第 6 行**
- **问题**: Unable to import 'torch'
- **原代码**:
  ```python
  import torch
  ```

**5. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'torch'
- **原代码**:
  ```python
  from torch import Tensor
  ```

**6. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'torch.nn'
- **原代码**:
  ```python
  from torch.nn import Module
  ```

**7. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'torch.distributed'
- **原代码**:
  ```python
  import torch.distributed as dist
  ```

#### 📄 `docker_build.py`

**1. [PY100] 第 29 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__(message)
  ```

**2. [PY100] 第 30 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  self.super_str = super().__str__()
  ```

**3. [PY100] 第 388 行**
- **问题**: 使用了未定义的名称 'build_instance_image'。
- **原代码**:
  ```python
  successful, failed = run_threadpool(build_instance_image, payloads, max_workers)
  ```

**4. [RUFF-E501] 第 94 行**
- **问题**: Line too long (106 > 88)
- **原代码**:
  ```python
  build_dir (Path): Directory for the build context (will also contain logs, scripts, and artifacts)
  ```

**5. [RUFF-E501] 第 125 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  f"Building docker image {image_name} in {build_dir} with platform {platform}"
  ```

**6. [RUFF-E501] 第 176 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  force_rebuild (bool): Whether to force rebuild the images even if they already exist
  ```

**7. [RUFF-E501] 第 223 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  Returns a dictionary of image names to build scripts and dockerfiles for environment images.
  ```

**8. [RUFF-F841] 第 246 行**
- **问题**: Local variable `base_image` is assigned to but never used
- **原代码**:
  ```python
  base_image = base_images[test_spec.base_image_key]
  ```

**9. [RUFF-E501] 第 249 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  f"Base image {test_spec.base_image_key} not found for {test_spec.env_image_key}\n."
  ```

**10. [RUFF-F841] 第 256 行**
- **问题**: Local variable `env_image` is assigned to but never used
- **原代码**:
  ```python
  env_image = client.images.get(test_spec.env_image_key)
  ```

**11. [RUFF-E501] 第 285 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  force_rebuild (bool): Whether to force rebuild the images even if they already exist
  ```

**12. [RUFF-E501] 第 351 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  force_rebuild (bool): Whether to force rebuild the images even if they already exist
  ```

**13. [RUFF-F841] 第 430 行**
- **问题**: Local variable `env_image` is assigned to but never used
- **原代码**:
  ```python
  env_image = client.images.get(env_image_name)
  ```

**14. [RUFF-E501] 第 479 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  Builds the instance image for the given test spec and creates a container from the image.
  ```

**15. [RUFF-E501] 第 483 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  client (docker.DockerClient): Docker client for building image + creating the container
  ```

**16. [RUFF-E501] 第 487 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  force_rebuild (bool): Whether to force rebuild the image even if it already exists
  ```

**17. [RUFF-E501] 第 504 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  f"Error occurred while pulling image {test_spec.base_image_key}: {str(e)}"
  ```

**18. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

**19. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'docker.errors'
- **原代码**:
  ```python
  import docker.errors
  ```

**20. [PL-import-error] 第 11 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**21. [PL-import-error] 第 18 行**
- **问题**: Unable to import 'swebench.harness.docker_utils'
- **原代码**:
  ```python
  from swebench.harness.docker_utils import cleanup_container, remove_image
  ```

**22. [PL-import-error] 第 19 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import (
  ```

**23. [PL-import-error] 第 24 行**
- **问题**: Unable to import 'swebench.harness.utils'
- **原代码**:
  ```python
  from swebench.harness.utils import ansi_escape, run_threadpool
  ```

#### 📄 `docker_utils.py`

**1. [PY100] 第 285 行**
- **问题**: 使用了未定义的名称 'should_remove'。
- **原代码**:
  ```python
  if should_remove(image_name, cache_level, clean, prior_images):
  ```

**2. [RUFF-E731] 第 80 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  log_info = lambda x: None
  ```

**3. [RUFF-E731] 第 81 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  log_error = lambda x: None
  ```

**4. [RUFF-E731] 第 122 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  log_info = lambda x: None
  ```

**5. [RUFF-E731] 第 123 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  log_error = lambda x: None
  ```

**6. [RUFF-E501] 第 138 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  f"Failed to stop container {container.name}: {e}. Trying to forcefully kill..."
  ```

**7. [RUFF-E501] 第 276 行**
- **问题**: Line too long (109 > 88)
- **原代码**:
  ```python
  clean (bool): Whether to clean; remove images that are higher in the cache hierarchy than the current
  ```

**8. [RUFF-E501] 第 277 行**
- **问题**: Line too long (108 > 88)
- **原代码**:
  ```python
  cache level. E.g. if cache_level is set to env, remove all previously built instances images. if
  ```

**9. [RUFF-E501] 第 278 行**
- **问题**: Line too long (108 > 88)
- **原代码**:
  ```python
  clean is false, previously built instances images will not be removed, but instance images built
  ```

**10. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

**11. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'docker.errors'
- **原代码**:
  ```python
  import docker.errors
  ```

**12. [PL-import-error] 第 13 行**
- **问题**: Unable to import 'docker.models.containers'
- **原代码**:
  ```python
  from docker.models.containers import Container
  ```

**13. [PL-no-member] 第 150 行**
- **问题**: Module 'signal' has no 'SIGKILL' member; maybe 'SIGILL'?
- **原代码**:
  ```python
  os.kill(pid, signal.SIGKILL)
  ```

#### 📄 `eval_retrieval.py`

**1. [RUFF-E501] 第 3 行**
- **问题**: Line too long (175 > 88)
- **原代码**:
  ```python
  """This script can be used to evaluate the BM25 retrieval results for a dataset created with create_text_dataset.py with the --retrieval_file option and --file_source bm25."""
  ```

**2. [RUFF-E722] 第 21 行**
- **问题**: Do not use bare `except`
- **原代码**:
  ```python
  except:
  ```

**3. [PL-import-error] 第 7 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import load_dataset, disable_caching, load_from_disk
  ```

#### 📄 `get_tasks_pipeline.py`

**1. [RUFF-E501] 第 75 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  f"📁 Pull request data for {repo} already exists at {path_pr}, skipping..."
  ```

**2. [RUFF-E501] 第 83 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  f"✅ Successfully saved task instance data for {repo} to {path_task}"
  ```

**3. [RUFF-E501] 第 87 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  f"📁 Task instance data for {repo} already exists at {path_task}, skipping..."
  ```

**4. [RUFF-E501] 第 121 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  "Missing GITHUB_TOKENS, consider rerunning with GITHUB_TOKENS=$(gh auth token)"
  ```

**5. [RUFF-E501] 第 147 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  help="List of repositories (e.g., `sqlfluff/sqlfluff`) to create task instances for",
  ```

**6. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'dotenv'
- **原代码**:
  ```python
  from dotenv import load_dotenv
  ```

**7. [PL-import-error] 第 11 行**
- **问题**: Unable to import 'swebench.collect.build_dataset'
- **原代码**:
  ```python
  from swebench.collect.build_dataset import main as build_dataset
  ```

**8. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'swebench.collect.print_pulls'
- **原代码**:
  ```python
  from swebench.collect.print_pulls import main as print_pulls
  ```

#### 📄 `get_top_pypi.py`

**1. [PY100] 第 48 行**
- **问题**: 使用了未定义的名称 'driver'。
- **原代码**:
  ```python
  driver.get(package_url)
  ```

**2. [PY100] 第 49 行**
- **问题**: 使用了未定义的名称 'driver'。
- **原代码**:
  ```python
  soup = BeautifulSoup(driver.page_source, "html.parser")
  ```

**3. [RUFF-E722] 第 74 行**
- **问题**: Do not use bare `except`
- **原代码**:
  ```python
  except:
  ```

**4. [PL-import-error] 第 7 行**
- **问题**: Unable to import 'bs4'
- **原代码**:
  ```python
  from bs4 import BeautifulSoup
  ```

**5. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'ghapi.core'
- **原代码**:
  ```python
  from ghapi.core import GhApi
  ```

**6. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'selenium'
- **原代码**:
  ```python
  from selenium import webdriver
  ```

**7. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'selenium.webdriver.common.by'
- **原代码**:
  ```python
  from selenium.webdriver.common.by import By
  ```

**8. [PL-possibly-used-before-assignment] 第 48 行**
- **问题**: Possibly using variable 'driver' before assignment
- **原代码**:
  ```python
  driver.get(package_url)
  ```

#### 📄 `get_versions.py`

**1. [PY100] 第 76 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**2. [PY100] 第 76 行**
- **问题**: 使用了未定义的名称 'sep'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**3. [PY003] 第 158 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(
  ```

**4. [PY003] 第 161 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(
  ```

**5. [PY003] 第 164 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(
  ```

**6. [PY003] 第 167 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  out_check = subprocess.run(
  ```

**7. [PY003] 第 177 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  out_install = subprocess.run(
  ```

**8. [PY100] 第 277 行**
- **问题**: 使用了未定义的名称 'data_task_list'。
- **原代码**:
  ```python
  "data_tasks": data_task_list,
  ```

**9. [PY100] 第 278 行**
- **问题**: 使用了未定义的名称 'i'。
- **原代码**:
  ```python
  "save_path": f"{repo_prefix}_versions_{i}.json"
  ```

**10. [PY100] 第 280 行**
- **问题**: 使用了未定义的名称 'i'。
- **原代码**:
  ```python
  else f"{repo_prefix}_versions_{i}_web.json",
  ```

**11. [PY003] 第 324 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(cmd_clone, shell=True, check=True, stdout=subprocess.DEVNULL)
  ```

**12. [PY003] 第 334 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(
  ```

**13. [PY003] 第 382 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(f"rm -rf {testbed_repo_name}", shell=True, check=True)
  ```

**14. [PY003] 第 388 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(cmd_rm_env, shell=True, check=True)
  ```

**15. [RUFF-E731] 第 76 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**16. [RUFF-E501] 第 265 行**
- **问题**: Line too long (110 > 88)
- **原代码**:
  ```python
  f"Split instances into {len(data_task_lists)} groups with lengths {[len(x) for x in data_task_lists]}"
  ```

**17. [RUFF-E501] 第 268 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  # If retrieval method includes GitHub, then search GitHub for versions via parallel call
  ```

**18. [RUFF-E501] 第 304 行**
- **问题**: Line too long (128 > 88)
- **原代码**:
  ```python
  f"Split instances into {len(data_task_lists)} groups with lengths {[len(x) for x in data_task_lists]} for build"
  ```

**19. [RUFF-E501] 第 327 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  f"Repo for {data_tasks[0]['repo']} exists: {testbed_repo_name}; skipping..."
  ```

**20. [RUFF-E501] 第 333 行**
- **问题**: Line too long (102 > 88)
- **原代码**:
  ```python
  cmd_clone_env = f"{conda_exec} create --name {conda_env_name} --clone {args.conda_env} -y"
  ```

**21. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'swebench.versioning.constants'
- **原代码**:
  ```python
  from swebench.versioning.constants import (
  ```

**22. [PL-import-error] 第 17 行**
- **问题**: Unable to import 'swebench.versioning.utils'
- **原代码**:
  ```python
  from swebench.versioning.utils import get_instances, split_instances
  ```

**23. [B602] 第 169 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  shell=True,
  ```

**24. [B602] 第 179 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  shell=True,
  ```

**25. [B602] 第 324 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  subprocess.run(cmd_clone, shell=True, check=True, stdout=subprocess.DEVNULL)
  ```

**26. [B602] 第 335 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  cmd_clone_env, shell=True, check=True, stdout=subprocess.DEVNULL
  ```

**27. [B602] 第 382 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  subprocess.run(f"rm -rf {testbed_repo_name}", shell=True, check=True)
  ```

**28. [B602] 第 388 行**
- **问题**: subprocess call with shell=True identified, security issue.
- **原代码**:
  ```python
  subprocess.run(cmd_rm_env, shell=True, check=True)
  ```

#### 📄 `get_versions_astropy.py`

**1. [PY100] 第 27 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**2. [PY100] 第 27 行**
- **问题**: 使用了未定义的名称 'sep'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**3. [PY100] 第 46 行**
- **问题**: 使用了未定义的名称 'k'。
- **原代码**:
  ```python
  version_to_time = [(k, max(v)) for k, v in map_version_to_times.items()]
  ```

**4. [PY100] 第 46 行**
- **问题**: 使用了未定义的名称 'v'。
- **原代码**:
  ```python
  version_to_time = [(k, max(v)) for k, v in map_version_to_times.items()]
  ```

**5. [PY100] 第 47 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  version_to_time = sorted(version_to_time, key=lambda x: x[0])[::-1]
  ```

**6. [RUFF-E731] 第 27 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**7. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'utils'
- **原代码**:
  ```python
  from utils import get_instances
  ```

#### 📄 `get_versions_matplotlib.py`

**1. [PY100] 第 25 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**2. [PY100] 第 25 行**
- **问题**: 使用了未定义的名称 'sep'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**3. [PY100] 第 36 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  times = sorted(times, key=lambda x: x[0])[::-1]
  ```

**4. [RUFF-E501] 第 19 行**
- **问题**: Line too long (119 > 88)
- **原代码**:
  ```python
  pattern = r'<a class="reference internal" href="prev_whats_new/whats_new_(.*).html">What\'s new in Matplotlib (.*)</a>'
  ```

**5. [RUFF-E731] 第 25 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**6. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'utils'
- **原代码**:
  ```python
  from utils import get_instances
  ```

#### 📄 `get_versions_pvlib-python.py`

**1. [PY100] 第 28 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**2. [PY100] 第 28 行**
- **问题**: 使用了未定义的名称 'sep'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**3. [PY100] 第 47 行**
- **问题**: 使用了未定义的名称 'k'。
- **原代码**:
  ```python
  version_to_time = [(k, max(v)) for k, v in map_version_to_times.items()]
  ```

**4. [PY100] 第 47 行**
- **问题**: 使用了未定义的名称 'v'。
- **原代码**:
  ```python
  version_to_time = [(k, max(v)) for k, v in map_version_to_times.items()]
  ```

**5. [PY100] 第 48 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  version_to_time = sorted(version_to_time, key=lambda x: x[0])[::-1]
  ```

**6. [RUFF-E731] 第 28 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**7. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'utils'
- **原代码**:
  ```python
  from utils import get_instances
  ```

#### 📄 `get_versions_pydicom.py`

**1. [PY100] 第 31 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  times = sorted(times, key=lambda x: x[0], reverse=True)
  ```

**2. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'bs4'
- **原代码**:
  ```python
  from bs4 import BeautifulSoup
  ```

**3. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'utils'
- **原代码**:
  ```python
  from utils import get_instances
  ```

**4. [PL-no-member] 第 27 行**
- **问题**: Module 'datetime' has no 'strptime' member
- **原代码**:
  ```python
  date = datetime.strptime(date, "%B %Y").strftime("%Y-%m-%d")
  ```

#### 📄 `get_versions_sqlfluff.py`

**1. [PY012] 第 10 行**
- **问题**: 疑似硬编码凭据变量：GITHUB_TOKEN。
- **原代码**:
  ```python
  GITHUB_TOKEN = "<your GitHub token>"
  ```

**2. [PY100] 第 68 行**
- **问题**: 使用了未定义的名称 'v'。
- **原代码**:
  ```python
  times = [(v, k) for k, v in version_date_map.items()]
  ```

**3. [PY100] 第 68 行**
- **问题**: 使用了未定义的名称 'k'。
- **原代码**:
  ```python
  times = [(v, k) for k, v in version_date_map.items()]
  ```

**4. [RUFF-E711] 第 56 行**
- **问题**: Comparison to `None` should be `cond is None`
- **原代码**:
  ```python
  if pair_rv[0] == None:
  ```

**5. [RUFF-E711] 第 61 行**
- **问题**: Comparison to `None` should be `cond is None`
- **原代码**:
  ```python
  date = pair[1] if pair_rv[1] == None else pair_rv[1]
  ```

**6. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'ghapi.core'
- **原代码**:
  ```python
  from ghapi.core import GhApi
  ```

**7. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'utils'
- **原代码**:
  ```python
  from utils import get_instances
  ```

#### 📄 `get_versions_xarray.py`

**1. [PY100] 第 28 行**
- **问题**: 使用了未定义的名称 'sep'。
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**2. [RUFF-E731] 第 28 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  keep_major_minor = lambda x, sep: ".".join(x.strip().split(sep)[:2])
  ```

**3. [RUFF-E722] 第 40 行**
- **问题**: Do not use bare `except`
- **原代码**:
  ```python
  except:
  ```

**4. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'utils'
- **原代码**:
  ```python
  from utils import get_instances
  ```

#### 📄 `go.py`

**1. [PL-import-error] 第 2 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import TestStatus
  ```

**2. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

#### 📄 `grading.py`

**1. [PY100] 第 63 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: x in content,
  ```

**2. [RUFF-E501] 第 88 行**
- **问题**: Line too long (102 > 88)
- **原代码**:
  ```python
  # This handles cases where pytest output goes to stderr and isn't captured between markers
  ```

**3. [RUFF-E501] 第 221 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  - If (fail-to-pass (Resolution) < 1 and > 0) and pass-to-pass (Maintenance) = 1 -> PARTIAL
  ```

**4. [RUFF-E501] 第 246 行**
- **问题**: Line too long (101 > 88)
- **原代码**:
  ```python
  test_spec (dict): test spec containing keys "instance_id", "FAIL_TO_PASS", and "PASS_TO_PASS"
  ```

**5. [RUFF-E501] 第 247 行**
- **问题**: Line too long (108 > 88)
- **原代码**:
  ```python
  prediction (dict): prediction containing keys "instance_id", "model_name_or_path", and "model_patch"
  ```

**6. [RUFF-E501] 第 249 行**
- **问题**: Line too long (102 > 88)
- **原代码**:
  ```python
  include_tests_status (bool): whether to include the status of each test in the returned report
  ```

**7. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**8. [PL-import-error] 第 22 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

**9. [PL-import-error] 第 23 行**
- **问题**: Unable to import 'swebench.harness.log_parsers'
- **原代码**:
  ```python
  from swebench.harness.log_parsers import MAP_REPO_TO_PARSER
  ```

#### 📄 `java.py`

**1. [PY100] 第 11 行**
- **问题**: 使用了未定义的名称 'test'。
- **原代码**:
  ```python
  tests_xml = "\n".join(rf'<test name="{test}" />' for test in tests)
  ```

**2. [PL-import-error] 第 2 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import TestStatus
  ```

**3. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

#### 📄 `javascript.py`

**1. [PY100] 第 16 行**
- **问题**: 使用了未定义的名称 'match_pattern'。
- **原代码**:
  ```python
  [" - ".join([x[0] for x in suite]), re.match(match_pattern, line).group(1)]
  ```

**2. [PY100] 第 16 行**
- **问题**: 使用了未定义的名称 'line'。
- **原代码**:
  ```python
  [" - ".join([x[0] for x in suite]), re.match(match_pattern, line).group(1)]
  ```

**3. [RUFF-E501] 第 34 行**
- **问题**: Line too long (100 > 88)
- **原代码**:
  ```python
  f"./node_modules/.bin/jest --verbose -c=test/{pkg}/jest.config.js '{test_path}'"
  ```

**4. [RUFF-E501] 第 49 行**
- **问题**: Line too long (102 > 88)
- **原代码**:
  ```python
  f"./node_modules/.bin/jest --verbose -c=test/{pkg}/jest.config.json '{test_path}'"
  ```

**5. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**6. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'swebench.harness.test_spec.utils'
- **原代码**:
  ```python
  from swebench.harness.test_spec.utils import make_eval_script_list_common
  ```

**7. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'unidiff'
- **原代码**:
  ```python
  from unidiff import PatchSet
  ```

#### 📄 `make_lite.py`

**1. [PL-import-error] 第 1 行**
- **问题**: Unable to import 'criteria'
- **原代码**:
  ```python
  from criteria import (
  ```

**2. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import load_dataset, disable_caching, DatasetDict
  ```

#### 📄 `modeling_flash_llama.py`

**1. [PY100] 第 71 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**2. [PY100] 第 126 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**3. [PY100] 第 256 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**4. [PY100] 第 287 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**5. [PY100] 第 465 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**6. [PY100] 第 555 行**
- **问题**: 使用了未定义的名称 'LlamaModel'。
- **原代码**:
  ```python
  if isinstance(module, LlamaModel):
  ```

**7. [PY100] 第 568 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__(config)
  ```

**8. [PY100] 第 741 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__(config)
  ```

**9. [PY100] 第 912 行**
- **问题**: 使用了未定义的名称 'past_state'。
- **原代码**:
  ```python
  past_state.index_select(0, beam_idx.to(past_state.device))
  ```

**10. [PY100] 第 912 行**
- **问题**: 使用了未定义的名称 'past_state'。
- **原代码**:
  ```python
  past_state.index_select(0, beam_idx.to(past_state.device))
  ```

**11. [PY100] 第 906 行**
- **问题**: 使用了未定义的名称 'staticmethod'。
- **原代码**:
  ```python
  @staticmethod
  ```

**12. [PY100] 第 921 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__(config)
  ```

**13. [RUFF-E501] 第 112 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  interleaved: if True, rotate pairs of even and odd dimensions (GPT-J style) instead
  ```

**14. [RUFF-E501] 第 114 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  pos_idx_in_fp32: if True, the position indices [0.0, ..., seqlen - 1] are in fp32,
  ```

**15. [RUFF-E501] 第 116 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  This option was added because previously (before 2023-07-02), when we construct
  ```

**16. [RUFF-E501] 第 117 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  the position indices, we use the dtype of self.inv_freq. In most cases this would
  ```

**17. [RUFF-E501] 第 118 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  be fp32, but if the model is trained in pure bf16 (not mixed precision), then
  ```

**18. [RUFF-E501] 第 120 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  Because of the limited precision of bf16 (e.g. 1995.0 is rounded to 2000.0), the
  ```

**19. [RUFF-E501] 第 170 行**
- **问题**: Line too long (97 > 88)
- **原代码**:
  ```python
  # We want fp32 here, not self.inv_freq.dtype, since the model could be loaded in bf16
  ```

**20. [RUFF-E501] 第 171 行**
- **问题**: Line too long (97 > 88)
- **原代码**:
  ```python
  # And the output of arange can be quite large, so bf16 would lose a lot of precision.
  ```

**21. [RUFF-E501] 第 172 行**
- **问题**: Line too long (100 > 88)
- **原代码**:
  ```python
  # However, for compatibility reason, we add an option to use the dtype of self.inv_freq.
  ```

**22. [RUFF-E501] 第 176 行**
- **问题**: Line too long (100 > 88)
- **原代码**:
  ```python
  # We want fp32 here as well since inv_freq will be multiplied with t, and the output
  ```

**23. [RUFF-E501] 第 177 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  # will be large. Having it in bf16 will lose a lot of precision and cause the
  ```

**24. [RUFF-E501] 第 218 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  seqlen_offset: can be used in generation where the qkv being passed in is only the last
  ```

**25. [RUFF-E501] 第 272 行**
- **问题**: Line too long (113 > 88)
- **原代码**:
  ```python
  This is the equivalent of torch.repeat_interleave(x, dim=1, repeats=n_rep). The hidden states go from (batch,
  ```

**26. [RUFF-E501] 第 273 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  num_key_value_heads, seqlen, head_dim) to (batch, num_attention_heads, seqlen, head_dim)
  ```

**27. [RUFF-E501] 第 300 行**
- **问题**: Line too long (100 > 88)
- **原代码**:
  ```python
  f"hidden_size must be divisible by num_heads (got `hidden_size`: {self.hidden_size}"
  ```

**28. [RUFF-E501] 第 368 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  # NOTE: Hack to include position_ids, assuming they are increasing uniformly per block
  ```

**29. [RUFF-E501] 第 412 行**
- **问题**: Line too long (112 > 88)
- **原代码**:
  ```python
  # NOTE: we assume that padding tokens are at the end of the sequence and may ignore `attention_mask`
  ```

**30. [RUFF-E501] 第 425 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  # varlen, ignore padding tokens, efficient for large batch with many paddings
  ```

**31. [RUFF-E501] 第 490 行**
- **问题**: Line too long (106 > 88)
- **原代码**:
  ```python
  hidden_states (`torch.FloatTensor`): input to the layer of shape `(batch, seq_len, embed_dim)`
  ```

**32. [RUFF-E501] 第 492 行**
- **问题**: Line too long (114 > 88)
- **原代码**:
  ```python
  `(batch, 1, tgt_len, src_len)` where padding elements are indicated by very large negative values.
  ```

**33. [RUFF-E501] 第 494 行**
- **问题**: Line too long (111 > 88)
- **原代码**:
  ```python
  Whether or not to return the attentions tensors of all attention layers. See `attentions` under
  ```

**34. [RUFF-E501] 第 497 行**
- **问题**: Line too long (118 > 88)
- **原代码**:
  ```python
  If set to `True`, `past_key_values` key value states are returned and can be used to speed up decoding
  ```

**35. [RUFF-E501] 第 499 行**
- **问题**: Line too long (112 > 88)
- **原代码**:
  ```python
  past_key_value (`Tuple(torch.FloatTensor)`, *optional*): cached past key and value projection states
  ```

**36. [RUFF-E501] 第 561 行**
- **问题**: Line too long (110 > 88)
- **原代码**:
  ```python
  Transformer decoder consisting of *config.num_hidden_layers* layers. Each layer is a [`LlamaDecoderLayer`]
  ```

**37. [RUFF-E501] 第 622 行**
- **问题**: Line too long (102 > 88)
- **原代码**:
  ```python
  "You cannot specify both decoder_input_ids and decoder_inputs_embeds at the same time"
  ```

**38. [RUFF-E501] 第 644 行**
- **问题**: Line too long (112 > 88)
- **原代码**:
  ```python
  "`use_cache=True` is incompatible with gradient checkpointing. Setting `use_cache=False`..."
  ```

**39. [RUFF-E501] 第 785 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  labels (`torch.LongTensor` of shape `(batch_size, sequence_length)`, *optional*):
  ```

**40. [RUFF-E501] 第 786 行**
- **问题**: Line too long (109 > 88)
- **原代码**:
  ```python
  Labels for computing the masked language modeling loss. Indices should either be in `[0, ...,
  ```

**41. [RUFF-E501] 第 787 行**
- **问题**: Line too long (118 > 88)
- **原代码**:
  ```python
  config.vocab_size]` or -100 (see `input_ids` docstring). Tokens with indices set to `-100` are ignored
  ```

**42. [RUFF-E501] 第 788 行**
- **问题**: Line too long (112 > 88)
- **原代码**:
  ```python
  (masked), the loss is only computed for the tokens with labels in `[0, ..., config.vocab_size]`.
  ```

**43. [RUFF-E501] 第 805 行**
- **问题**: Line too long (113 > 88)
- **原代码**:
  ```python
  >>> tokenizer.batch_decode(generate_ids, skip_special_tokens=True, clean_up_tokenization_spaces=False)[0]
  ```

**44. [RUFF-E501] 第 806 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  "Hey, are you conscious? Can you talk to me?\nI'm not conscious, but I can talk to you."
  ```

**45. [RUFF-E501] 第 852 行**
- **问题**: Line too long (108 > 88)
- **原代码**:
  ```python
  # Don't take mean since this will give unequal weight to GPUs with unequal amount of padding
  ```

**46. [RUFF-E501] 第 886 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  # if `inputs_embeds` are passed, we only want to use them in the 1st generation step
  ```

**47. [RUFF-E501] 第 950 行**
- **问题**: Line too long (108 > 88)
- **原代码**:
  ```python
  Labels for computing the sequence classification/regression loss. Indices should be in `[0, ...,
  ```

**48. [RUFF-E501] 第 951 行**
- **问题**: Line too long (117 > 88)
- **原代码**:
  ```python
  config.num_labels - 1]`. If `config.num_labels == 1` a regression loss is computed (Mean-Square loss), If
  ```

**49. [PL-import-error] 第 23 行**
- **问题**: Unable to import 'torch'
- **原代码**:
  ```python
  import torch
  ```

**50. [PL-import-error] 第 24 行**
- **问题**: Unable to import 'torch.nn.functional'
- **原代码**:
  ```python
  import torch.nn.functional as F
  ```

**51. [PL-import-error] 第 25 行**
- **问题**: Unable to import 'torch.utils.checkpoint'
- **原代码**:
  ```python
  import torch.utils.checkpoint
  ```

**52. [PL-import-error] 第 26 行**
- **问题**: Unable to import 'torch'
- **原代码**:
  ```python
  from torch import nn
  ```

**53. [PL-import-error] 第 27 行**
- **问题**: Unable to import 'torch.nn'
- **原代码**:
  ```python
  from torch.nn import BCEWithLogitsLoss, CrossEntropyLoss, MSELoss
  ```

**54. [PL-import-error] 第 29 行**
- **问题**: Unable to import 'torch.distributed'
- **原代码**:
  ```python
  import torch.distributed as dist
  ```

**55. [PL-import-error] 第 31 行**
- **问题**: Unable to import 'transformers'
- **原代码**:
  ```python
  from transformers import GenerationMixin
  ```

**56. [PL-import-error] 第 32 行**
- **问题**: Unable to import 'transformers.activations'
- **原代码**:
  ```python
  from transformers.activations import ACT2FN
  ```

**57. [PL-import-error] 第 33 行**
- **问题**: Unable to import 'transformers.modeling_outputs'
- **原代码**:
  ```python
  from transformers.modeling_outputs import (
  ```

**58. [PL-import-error] 第 38 行**
- **问题**: Unable to import 'transformers.modeling_utils'
- **原代码**:
  ```python
  from transformers.modeling_utils import PreTrainedModel
  ```

**59. [PL-import-error] 第 39 行**
- **问题**: Unable to import 'transformers.utils'
- **原代码**:
  ```python
  from transformers.utils import logging
  ```

**60. [PL-import-error] 第 40 行**
- **问题**: Unable to import 'transformers.models.llama.configuration_llama'
- **原代码**:
  ```python
  from transformers.models.llama.configuration_llama import LlamaConfig
  ```

**61. [PL-import-error] 第 42 行**
- **问题**: Unable to import 'swebench.inference.llamao.distributed_attention'
- **原代码**:
  ```python
  from swebench.inference.llamao.distributed_attention import DistributedAttention
  ```

**62. [PL-import-error] 第 43 行**
- **问题**: Unable to import 'flash_attn'
- **原代码**:
  ```python
  from flash_attn import flash_attn_kvpacked_func, flash_attn_varlen_kvpacked_func
  ```

**63. [PL-import-error] 第 44 行**
- **问题**: Unable to import 'flash_attn.bert_padding'
- **原代码**:
  ```python
  from flash_attn.bert_padding import unpad_input, pad_input
  ```

**64. [PL-possibly-used-before-assignment] 第 671 行**
- **问题**: Possibly using variable 'unpad_indices' before assignment
- **原代码**:
  ```python
  pad_input(hidden_states, unpad_indices, bsz, max_seqlen),
  ```

#### 📄 `php.py`

**1. [PL-import-error] 第 2 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import TestStatus
  ```

**2. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

#### 📄 `prepare_images.py`

**1. [RUFF-E501] 第 50 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  # Check if the instance needs to be built (based on force_rebuild flag and existing images)
  ```

**2. [PL-import-error] 第 1 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

**3. [PL-import-error] 第 2 行**
- **问题**: Unable to import 'resource'
- **原代码**:
  ```python
  import resource
  ```

**4. [PL-import-error] 第 6 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import KEY_INSTANCE_ID
  ```

**5. [PL-import-error] 第 7 行**
- **问题**: Unable to import 'swebench.harness.docker_build'
- **原代码**:
  ```python
  from swebench.harness.docker_build import build_instance_images
  ```

**6. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'swebench.harness.docker_utils'
- **原代码**:
  ```python
  from swebench.harness.docker_utils import list_images
  ```

**7. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import make_test_spec
  ```

**8. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'swebench.harness.utils'
- **原代码**:
  ```python
  from swebench.harness.utils import load_swebench_dataset, str2bool, optional_str
  ```

#### 📄 `print_pulls.py`

**1. [RUFF-E501] 第 3 行**
- **问题**: Line too long (137 > 88)
- **原代码**:
  ```python
  """Given the `<owner/name>` of a GitHub repo, this script writes the raw information for all the repo's PRs to a single `.jsonl` file."""
  ```

**2. [PL-import-error] 第 13 行**
- **问题**: Unable to import 'fastcore.xtras'
- **原代码**:
  ```python
  from fastcore.xtras import obj2dict
  ```

**3. [PL-import-error] 第 14 行**
- **问题**: Unable to import 'swebench.collect.utils'
- **原代码**:
  ```python
  from swebench.collect.utils import Repo
  ```

#### 📄 `python.py`

**1. [PY100] 第 90 行**
- **问题**: 使用了未定义的名称 'l'。
- **原代码**:
  ```python
  len(l) + 1 for l in lines_after_pip[:ix]
  ```

**2. [PY100] 第 248 行**
- **问题**: 使用了未定义的名称 'ext'。
- **原代码**:
  ```python
  d for d in directives if not any(d.endswith(ext) for ext in NON_TEST_EXTS)
  ```

**3. [RUFF-E501] 第 22 行**
- **问题**: Line too long (141 > 88)
- **原代码**:
  ```python
  "User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_11_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/50.0.2661.102 Safari/537.36"
  ```

**4. [RUFF-E501] 第 40 行**
- **问题**: Line too long (123 > 88)
- **原代码**:
  ```python
  f"Could not find environment.yml at paths {MAP_REPO_TO_ENV_YML_PATHS[repo]} for repo {repo} at commit {commit}"
  ```

**5. [RUFF-F841] 第 75 行**
- **问题**: Local variable `pip_line_start` is assigned to but never used
- **原代码**:
  ```python
  pip_line_start = pip_match.start()
  ```

**6. [RUFF-E501] 第 79 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  # find where pip section ends by looking for a line that's at same or less indentation
  ```

**7. [RUFF-E741] 第 90 行**
- **问题**: Ambiguous variable name: `l`
- **原代码**:
  ```python
  len(l) + 1 for l in lines_after_pip[:ix]
  ```

**8. [RUFF-E711] 第 99 行**
- **问题**: Comparison to `None` should be `cond is None`
- **原代码**:
  ```python
  if replacement == None:
  ```

**9. [RUFF-E501] 第 146 行**
- **问题**: Line too long (121 > 88)
- **原代码**:
  ```python
  f"Could not find requirements.txt at paths {MAP_REPO_TO_REQS_PATHS[repo]} for repo {repo} at commit {commit}"
  ```

**10. [RUFF-E731] 第 153 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  exclude_line = lambda line: any(
  ```

**11. [RUFF-E501] 第 188 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  E.g. types-pkg_resources has been yanked from PyPI, so we replace it with types-setuptools
  ```

**12. [RUFF-E711] 第 191 行**
- **问题**: Comparison to `None` should be `cond is None`
- **原代码**:
  ```python
  if replacement == None:
  ```

**13. [RUFF-E501] 第 251 行**
- **问题**: Line too long (107 > 88)
- **原代码**:
  ```python
  # For Django tests, remove extension + "tests/" prefix and convert slashes to dots (module referencing)
  ```

**14. [RUFF-E501] 第 274 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  f"git clone -o origin {branch} --single-branch https://github.com/{repo} {repo_directory}",
  ```

**15. [RUFF-E501] 第 282 行**
- **问题**: Line too long (211 > 88)
- **原代码**:
  ```python
  'git tag -l | while read tag; do TAG_COMMIT=$(git rev-list -n 1 "$tag"); TAG_TIME=$(git show -s --format=%ci "$TAG_COMMIT"); if [[ "$TAG_TIME" > "$TARGET_TIMESTAMP" ]]; then git tag -d "$tag"; fi; done',
  ```

**16. [RUFF-E501] 第 286 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  "AFTER_TIMESTAMP=$(date -d \"$TARGET_TIMESTAMP + 1 second\" '+%Y-%m-%d %H:%M:%S')",
  ```

**17. [RUFF-E501] 第 326 行**
- **问题**: Line too long (110 > 88)
- **原代码**:
  ```python
  f"cat <<'{HEREDOC_DELIMITER}' > /root/environment.yml\n{cached_environment_yml}\n{HEREDOC_DELIMITER}",
  ```

**18. [RUFF-E501] 第 385 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  cmd = f"conda activate {env_name} && conda install python={specs['python']} -y"
  ```

**19. [RUFF-E501] 第 434 行**
- **问题**: Line too long (89 > 88)
- **原代码**:
  ```python
  f"git config --global --add safe.directory {repo_directory}",  # for nonroot user
  ```

**20. [RUFF-E501] 第 451 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  reset_tests_command,  # Revert tests after done, leave the repo in the same state as before
  ```

**21. [PL-import-error] 第 6 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**22. [PL-import-error] 第 18 行**
- **问题**: Unable to import 'swebench.harness.utils'
- **原代码**:
  ```python
  from swebench.harness.utils import get_modified_files, load_cached_environment_yml
  ```

#### 📄 `remove_containers.py`

**1. [PL-import-error] 第 1 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

#### 📄 `remove_envs.py`

**1. [PY100] 第 50 行**
- **问题**: 使用了未定义的名称 'conda_source'。
- **原代码**:
  ```python
  cmd = conda_source + " && " + conda_cmd
  ```

**2. [RUFF-E501] 第 12 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  Parse conda environments (`conda env list`) created for a particular conda installation
  ```

**3. [RUFF-E501] 第 97 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  f"Removing miniconda folder for environments with {args.prefix} from {args.conda_path}"
  ```

**4. [PL-possibly-used-before-assignment] 第 50 行**
- **问题**: Possibly using variable 'conda_source' before assignment
- **原代码**:
  ```python
  cmd = conda_source + " && " + conda_cmd
  ```

#### 📄 `reporting.py`

**1. [PY100] 第 95 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  x,
  ```

**2. [PL-import-error] 第 1 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

**3. [PL-import-error] 第 6 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**4. [PL-import-error] 第 13 行**
- **问题**: Unable to import 'swebench.harness.docker_utils'
- **原代码**:
  ```python
  from swebench.harness.docker_utils import list_images
  ```

**5. [PL-import-error] 第 14 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import make_test_spec
  ```

#### 📄 `ruby.py`

**1. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import TestStatus
  ```

**2. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

#### 📄 `run_api.py`

**1. [PY100] 第 195 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: gpt_tokenize(x["text"], encoding) <= MODEL_LIMITS[model_name_or_path],
  ```

**2. [PY100] 第 350 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: claude_tokenize(x["text"], anthropic)
  ```

**3. [PY100] 第 488 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: x["instance_id"] not in existing_ids,
  ```

**4. [RUFF-E501] 第 3 行**
- **问题**: Line too long (139 > 88)
- **原代码**:
  ```python
  """This python script is designed to run inference on a dataset using either the OpenAI or Anthropic API, depending on the model specified.
  ```

**5. [RUFF-E501] 第 4 行**
- **问题**: Line too long (157 > 88)
- **原代码**:
  ```python
  It sorts instances by length and continually writes the outputs to a specified file, so that the script can be stopped and restarted without losing progress.
  ```

**6. [RUFF-E501] 第 345 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  "Must provide an api key. Expected in ANTHROPIC_API_KEY environment variable."
  ```

**7. [RUFF-E501] 第 411 行**
- **问题**: Line too long (99 > 88)
- **原代码**:
  ```python
  model_args (str): A string of comma-separated key-value pairs representing model arguments.
  ```

**8. [RUFF-E501] 第 528 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  help="Name of API model. Update MODEL* constants in this file to add new models.",
  ```

**9. [RUFF-E501] 第 554 行**
- **问题**: Line too long (97 > 88)
- **原代码**:
  ```python
  help="List of model arguments separated by commas. (e.g. 'top_p=0.95,temperature=0.70')",
  ```

**10. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'dotenv'
- **原代码**:
  ```python
  import dotenv
  ```

**11. [PL-import-error] 第 15 行**
- **问题**: Unable to import 'tiktoken'
- **原代码**:
  ```python
  import tiktoken
  ```

**12. [PL-import-error] 第 17 行**
- **问题**: Unable to import 'anthropic'
- **原代码**:
  ```python
  from anthropic import HUMAN_PROMPT, AI_PROMPT, Anthropic
  ```

**13. [PL-import-error] 第 18 行**
- **问题**: Unable to import 'tenacity'
- **原代码**:
  ```python
  from tenacity import (
  ```

**14. [PL-import-error] 第 23 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import load_dataset, load_from_disk
  ```

**15. [PL-import-error] 第 24 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.utils'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.utils import extract_diff
  ```

#### 📄 `run_evaluation.py`

**1. [PY100] 第 307 行**
- **问题**: 使用了未定义的名称 'instance'。
- **原代码**:
  ```python
  instance,
  ```

**2. [PY100] 第 459 行**
- **问题**: 使用了未定义的名称 'k'。
- **原代码**:
  ```python
  k
  ```

**3. [RUFF-E722] 第 137 行**
- **问题**: Do not use bare `except`
- **原代码**:
  ```python
  except:
  ```

**4. [RUFF-E501] 第 162 行**
- **问题**: Line too long (105 > 88)
- **原代码**:
  ```python
  f"Intermediate patch for {instance_id} written to {patch_file}, now applying to container..."
  ```

**5. [RUFF-E501] 第 201 行**
- **问题**: Line too long (92 > 88)
- **原代码**:
  ```python
  f"Eval script for {instance_id} written to {eval_file}; copying to container..."
  ```

**6. [RUFF-E501] 第 498 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  "⚠️ Local evaluation for the test split of SWE-bench Multimodal is not supported. "
  ```

**7. [RUFF-E501] 第 499 行**
- **问题**: Line too long (123 > 88)
- **原代码**:
  ```python
  "Please check out sb-cli (https://github.com/swe-bench/sb-cli/) for instructions on how to submit predictions."
  ```

**8. [RUFF-E501] 第 643 行**
- **问题**: Line too long (96 > 88)
- **原代码**:
  ```python
  # if clean is false, we only remove images above the cache level if they don't already exist
  ```

**9. [RUFF-E501] 第 667 行**
- **问题**: Line too long (103 > 88)
- **原代码**:
  ```python
  help="Doesn't run new instances, only writes reports for instances with existing test outputs",
  ```

**10. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

**11. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'resource'
- **原代码**:
  ```python
  import resource
  ```

**12. [PL-import-error] 第 16 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**13. [PL-import-error] 第 32 行**
- **问题**: Unable to import 'swebench.harness.docker_utils'
- **原代码**:
  ```python
  from swebench.harness.docker_utils import (
  ```

**14. [PL-import-error] 第 41 行**
- **问题**: Unable to import 'swebench.harness.docker_build'
- **原代码**:
  ```python
  from swebench.harness.docker_build import (
  ```

**15. [PL-import-error] 第 48 行**
- **问题**: Unable to import 'swebench.harness.grading'
- **原代码**:
  ```python
  from swebench.harness.grading import get_eval_report
  ```

**16. [PL-import-error] 第 49 行**
- **问题**: Unable to import 'swebench.harness.reporting'
- **原代码**:
  ```python
  from swebench.harness.reporting import make_run_report
  ```

**17. [PL-import-error] 第 50 行**
- **问题**: Unable to import 'swebench.harness.modal_eval'
- **原代码**:
  ```python
  from swebench.harness.modal_eval import (
  ```

**18. [PL-import-error] 第 54 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import make_test_spec, TestSpec
  ```

**19. [PL-import-error] 第 55 行**
- **问题**: Unable to import 'swebench.harness.utils'
- **原代码**:
  ```python
  from swebench.harness.utils import (
  ```

#### 📄 `run_evaluation_modal.py`

**1. [PY100] 第 159 行**
- **问题**: 使用了未定义的名称 'staticmethod'。
- **原代码**:
  ```python
  @staticmethod
  ```

**2. [RUFF-E402] 第 31 行**
- **问题**: Module level import not at top of file
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**3. [RUFF-E402] 第 36 行**
- **问题**: Module level import not at top of file
- **原代码**:
  ```python
  from swebench.harness.grading import get_eval_report
  ```

**4. [RUFF-E402] 第 37 行**
- **问题**: Module level import not at top of file
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import make_test_spec, TestSpec
  ```

**5. [RUFF-E501] 第 165 行**
- **问题**: Line too long (127 > 88)
- **原代码**:
  ```python
  "conda activate testbed && python -m pip install --trusted-host pypi-mirror.modal.local -r $HOME/requirements.txt",
  ```

**6. [RUFF-E501] 第 194 行**
- **问题**: Line too long (120 > 88)
- **原代码**:
  ```python
  "wget 'https://repo.anaconda.com/miniconda/Miniconda3-py311_23.11.0-2-Linux-x86_64.sh' -O miniconda.sh",
  ```

**7. [RUFF-E501] 第 210 行**
- **问题**: Line too long (114 > 88)
- **原代码**:
  ```python
  "echo 'source /opt/miniconda3/etc/profile.d/conda.sh && conda activate testbed' >> /root/.bashrc",
  ```

**8. [PL-import-error] 第 7 行**
- **问题**: Unable to import 'modal'
- **原代码**:
  ```python
  import modal
  ```

**9. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'modal.container_process'
- **原代码**:
  ```python
  import modal.container_process
  ```

**10. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'modal.io_streams'
- **原代码**:
  ```python
  import modal.io_streams
  ```

**11. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'tenacity'
- **原代码**:
  ```python
  import tenacity
  ```

**12. [PL-import-error] 第 16 行**
- **问题**: Unable to import 'swebench.harness.docker_build'
- **原代码**:
  ```python
  from swebench.harness.docker_build import setup_logger
  ```

**13. [PL-import-error] 第 17 行**
- **问题**: Unable to import 'swebench.harness.reporting'
- **原代码**:
  ```python
  from swebench.harness.reporting import make_run_report
  ```

**14. [PL-import-error] 第 18 行**
- **问题**: Unable to import 'swebench.harness.utils'
- **原代码**:
  ```python
  from swebench.harness.utils import EvaluationError
  ```

**15. [PL-import-error] 第 31 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**16. [PL-import-error] 第 36 行**
- **问题**: Unable to import 'swebench.harness.grading'
- **原代码**:
  ```python
  from swebench.harness.grading import get_eval_report
  ```

**17. [PL-import-error] 第 37 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import make_test_spec, TestSpec
  ```

#### 📄 `run_evaluation_modal_entrypoint.py`

**1. [PY001] 第 112 行**
- **问题**: 使用 exec 可能导致代码执行漏洞。
- **原代码**:
  ```python
  returncode = await exec(command)
  ```

**2. [PY100] 第 113 行**
- **问题**: 使用了未定义的名称 'exit'。
- **原代码**:
  ```python
  exit(returncode)
  ```

**3. [RUFF-E501] 第 3 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  # In a perfect world, we would execute commands using the Sandbox directly, but Modal imposes
  ```

**4. [RUFF-E501] 第 4 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  # a container stdio rate limit of 64 KiB/s. Some test harnesses exceed this limit which leads
  ```

**5. [RUFF-E501] 第 5 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  # to "dropped container output" logs that interfere with parsing the test output. Instead,
  ```

**6. [RUFF-E501] 第 6 行**
- **问题**: Line too long (95 > 88)
- **原代码**:
  ```python
  # we mount and run this script in the Sandbox to control the rate at which stdio is streamed to
  ```

**7. [RUFF-E501] 第 74 行**
- **问题**: Line too long (120 > 88)
- **原代码**:
  ```python
  # chance of errors of the form: "Error reading stream: 'utf-8' codec can't decode bytes in position ..."
  ```

#### 📄 `run_live.py`

**1. [PY100] 第 61 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = list(filter(lambda x: x.is_file(), files))
  ```

**2. [PY100] 第 62 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = list(filter(lambda x: x.name.lower().startswith("readme"), files))
  ```

**3. [PY100] 第 64 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = sorted(files, key=lambda x: len(x.name))
  ```

**4. [RUFF-F841] 第 107 行**
- **问题**: Local variable `thread_id` is assigned to but never used
- **原代码**:
  ```python
  thread_id = 0
  ```

**5. [RUFF-E501] 第 151 行**
- **问题**: Line too long (93 > 88)
- **原代码**:
  ```python
  f"Including {len(include_files)} files in context with {cur_input_len} tokens:\n"
  ```

**6. [RUFF-E501] 第 228 行**
- **问题**: Line too long (109 > 88)
- **原代码**:
  ```python
  f"Generated {response.usage.completion_tokens} tokens in {(time.time() - start):.2f} seconds"
  ```

**7. [PL-import-error] 第 13 行**
- **问题**: Unable to import 'ghapi.all'
- **原代码**:
  ```python
  from ghapi.all import GhApi
  ```

**8. [PL-import-error] 第 19 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.utils'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.utils import (
  ```

**9. [PL-import-error] 第 25 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.create_instance'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.create_instance import (
  ```

**10. [PL-import-error] 第 31 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.bm25_retrieval'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.bm25_retrieval import (
  ```

**11. [PL-import-error] 第 37 行**
- **问题**: Unable to import 'swebench.inference.run_api'
- **原代码**:
  ```python
  from swebench.inference.run_api import call_chat, call_anthropic
  ```

**12. [PL-import-error] 第 231 行**
- **问题**: Unable to import 'anthropic'
- **原代码**:
  ```python
  from anthropic import Anthropic
  ```

#### 📄 `run_llama.py`

**1. [PY100] 第 206 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: tokenizer(x["text"], truncation=False),
  ```

**2. [PY100] 第 213 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: {"input_ids": x["input_ids"] + [13]}, batched=False
  ```

**3. [PY100] 第 217 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  filter_func = lambda x: x >= min_len
  ```

**4. [PY100] 第 219 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  filter_func = lambda x: x < max_len
  ```

**5. [PY100] 第 221 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  filter_func = lambda x: min_len <= x < max_len
  ```

**6. [PY100] 第 224 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: filter_func(len(x["input_ids"])), desc="filtering for length"
  ```

**7. [PY100] 第 226 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lens = torch.tensor(list(map(lambda x: len(x["input_ids"]), dataset)))
  ```

**8. [PY100] 第 231 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: x["instance_id"] not in existing_ids,
  ```

**9. [PY100] 第 234 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lens = torch.tensor(list(map(lambda x: len(x["input_ids"]), dataset)))  # recompute
  ```

**10. [PY100] 第 266 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__()
  ```

**11. [RUFF-E501] 第 111 行**
- **问题**: Line too long (105 > 88)
- **原代码**:
  ```python
  peft_path (str or None): The path to the PEFT adapters. If None, no PEFT adapters will be loaded.
  ```

**12. [RUFF-E501] 第 122 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  k: f"{torch.cuda.get_device_properties(k).total_memory // 1_010_000_000:d}GIB"
  ```

**13. [RUFF-F841] 第 203 行**
- **问题**: Local variable `model_nickname` is assigned to but never used
- **原代码**:
  ```python
  model_nickname = "__".join(model_name_or_path.split("/")[-2:])
  ```

**14. [RUFF-E731] 第 217 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  filter_func = lambda x: x >= min_len
  ```

**15. [RUFF-E731] 第 219 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  filter_func = lambda x: x < max_len
  ```

**16. [RUFF-E731] 第 221 行**
- **问题**: Do not assign a `lambda` expression, use a `def`
- **原代码**:
  ```python
  filter_func = lambda x: min_len <= x < max_len
  ```

**17. [RUFF-E501] 第 237 行**
- **问题**: Line too long (146 > 88)
- **原代码**:
  ```python
  f"filtered dataset - {len(dataset)} examples, min length: {min(lens):_}, max length: {max(lens):_} (shard {shard_id} of {num_shards})"
  ```

**18. [RUFF-E501] 第 241 行**
- **问题**: Line too long (111 > 88)
- **原代码**:
  ```python
  f"filtered dataset - {len(dataset)} examples, min length: {min(lens):_}, max length: {max(lens):_}"
  ```

**19. [RUFF-E501] 第 262 行**
- **问题**: Line too long (115 > 88)
- **原代码**:
  ```python
  min_tokens (int): The minimum number of unique tokens required in the suffix of the generated sequence.
  ```

**20. [RUFF-E501] 第 316 行**
- **问题**: Line too long (116 > 88)
- **原代码**:
  ```python
  f"Generated {new_len} tokens ({total_len} total) in {(datetime.now() - start).total_seconds()} "
  ```

**21. [RUFF-E501] 第 317 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  + f"seconds (speed: {new_len / (datetime.now() - start).total_seconds()} tps)"
  ```

**22. [RUFF-E501] 第 382 行**
- **问题**: Line too long (132 > 88)
- **原代码**:
  ```python
  f"model_name_or_path {model_name_or_path} does not match peft_path base_model {peft_config.base_model_name_or_path}"
  ```

**23. [PL-import-error] 第 8 行**
- **问题**: Unable to import 'torch'
- **原代码**:
  ```python
  import torch
  ```

**24. [PL-import-error] 第 9 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import load_from_disk, load_dataset
  ```

**25. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'peft'
- **原代码**:
  ```python
  from peft import PeftConfig, PeftModel
  ```

**26. [PL-import-error] 第 12 行**
- **问题**: Unable to import 'transformers'
- **原代码**:
  ```python
  from transformers import (
  ```

**27. [PL-import-error] 第 17 行**
- **问题**: Unable to import 'swebench.inference.llamao.modeling_flash_llama'
- **原代码**:
  ```python
  from swebench.inference.llamao.modeling_flash_llama import (
  ```

**28. [PL-import-error] 第 20 行**
- **问题**: Unable to import 'swebench.inference.make_datasets.utils'
- **原代码**:
  ```python
  from swebench.inference.make_datasets.utils import extract_diff
  ```

#### 📄 `rust.py`

**1. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import TestStatus
  ```

**2. [PL-import-error] 第 4 行**
- **问题**: Unable to import 'swebench.harness.test_spec.test_spec'
- **原代码**:
  ```python
  from swebench.harness.test_spec.test_spec import TestSpec
  ```

#### 📄 `test_evaluation.py`

**1. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'docker'
- **原代码**:
  ```python
  import docker
  ```

**2. [PL-import-error] 第 5 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**3. [PL-import-error] 第 11 行**
- **问题**: Unable to import 'swebench.harness.run_evaluation'
- **原代码**:
  ```python
  from swebench.harness.run_evaluation import make_run_report
  ```

#### 📄 `test_harness_utils.py`

**1. [RUFF-E501] 第 18 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  We want to make sure that our cleaner only modifies the pip section of the environment.yml
  ```

**2. [RUFF-E501] 第 21 行**
- **问题**: Line too long (98 > 88)
- **原代码**:
  ```python
  We expect "types-pkg_resources" to be replaced with "types-setuptools" in the pip section.
  ```

**3. [RUFF-E501] 第 89 行**
- **问题**: Line too long (90 > 88)
- **原代码**:
  ```python
  """Test environment.yml cleaning with various version specifiers in pip section"""
  ```

**4. [PL-import-error] 第 2 行**
- **问题**: Unable to import 'swebench.harness.utils'
- **原代码**:
  ```python
  from swebench.harness.utils import run_threadpool
  ```

**5. [PL-import-error] 第 3 行**
- **问题**: Unable to import 'swebench.harness.test_spec.python'
- **原代码**:
  ```python
  from swebench.harness.test_spec.python import clean_environment_yml, clean_requirements
  ```

#### 📄 `test_spec.py`

**1. [PY100] 第 49 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**2. [PY100] 第 56 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**3. [PY100] 第 64 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**4. [PY100] 第 71 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**5. [PY100] 第 89 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**6. [PY100] 第 106 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**7. [PY100] 第 113 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**8. [PY100] 第 122 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**9. [PY100] 第 131 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**10. [PY100] 第 141 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**11. [PY100] 第 145 行**
- **问题**: 使用了未定义的名称 'property'。
- **原代码**:
  ```python
  @property
  ```

**12. [PY100] 第 168 行**
- **问题**: 使用了未定义的名称 'make_test_spec'。
- **原代码**:
  ```python
  lambda x: make_test_spec(x, namespace, instance_image_tag, env_image_tag),
  ```

**13. [PY100] 第 168 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: make_test_spec(x, namespace, instance_image_tag, env_image_tag),
  ```

**14. [RUFF-E501] 第 83 行**
- **问题**: Line too long (104 > 88)
- **原代码**:
  ```python
  ]  # 10 characters is still likely to be unique given only a few base images will be created
  ```

**15. [RUFF-E501] 第 84 行**
- **问题**: Line too long (100 > 88)
- **原代码**:
  ```python
  return f"sweb.base.{MAP_REPO_TO_EXT[self.repo]}.{self.arch}.{val}:{self.base_image_tag}"
  ```

**16. [RUFF-E501] 第 92 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  The key for the environment image is based on the hash of the environment script list.
  ```

**17. [RUFF-E501] 第 95 行**
- **问题**: Line too long (108 > 88)
- **原代码**:
  ```python
  Note that old images are not automatically deleted, so consider cleaning up old images periodically.
  ```

**18. [RUFF-E501] 第 104 行**
- **问题**: Line too long (94 > 88)
- **原代码**:
  ```python
  return f"sweb.env.{MAP_REPO_TO_EXT[self.repo]}.{self.arch}.{val}:{self.env_image_tag}"
  ```

**19. [RUFF-E501] 第 108 行**
- **问题**: Line too long (91 > 88)
- **原代码**:
  ```python
  key = f"sweb.eval.{self.arch}.{self.instance_id.lower()}:{self.instance_image_tag}"
  ```

**20. [RUFF-E501] 第 162 行**
- **问题**: Line too long (103 > 88)
- **原代码**:
  ```python
  Idempotent function that converts a list of SWEbenchInstance objects to a list of TestSpec objects.
  ```

**21. [RUFF-F841] 第 191 行**
- **问题**: Local variable `problem_statement` is assigned to but never used
- **原代码**:
  ```python
  problem_statement = instance.get("problem_statement")
  ```

**22. [RUFF-F841] 第 192 行**
- **问题**: Local variable `hints_text` is assigned to but never used
- **原代码**:
  ```python
  hints_text = instance.get("hints_text")  # Unused
  ```

**23. [PL-import-error] 第 7 行**
- **问题**: Unable to import 'swebench.harness.constants'
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```

**24. [PL-import-error] 第 15 行**
- **问题**: Unable to import 'swebench.harness.dockerfiles'
- **原代码**:
  ```python
  from swebench.harness.dockerfiles import (
  ```

**25. [PL-import-error] 第 20 行**
- **问题**: Unable to import 'swebench.harness.test_spec.create_scripts'
- **原代码**:
  ```python
  from swebench.harness.test_spec.create_scripts import (
  ```

#### 📄 `tokenize_dataset.py`

**1. [PY100] 第 137 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  lambda x: len(x["text"]) <= 5_000_000
  ```

**2. [PY100] 第 159 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  x, tokenizer_name, tokenizer, tokenizer_func, eos_token
  ```

**3. [PY100] 第 190 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  x, tokenizer_name, tokenizer, tokenizer_func, eos_token
  ```

**4. [RUFF-E501] 第 3 行**
- **问题**: Line too long (180 > 88)
- **原代码**:
  ```python
  """Provided a source (raw) directory and the final (eval) directory, create a training split by removing all instances that are in the final directory from the source directory."""
  ```

**5. [PL-import-error] 第 10 行**
- **问题**: Unable to import 'tiktoken'
- **原代码**:
  ```python
  import tiktoken
  ```

**6. [PL-import-error] 第 11 行**
- **问题**: Unable to import 'datasets'
- **原代码**:
  ```python
  from datasets import disable_caching, load_from_disk, load_dataset
  ```

**7. [PL-import-error] 第 13 行**
- **问题**: Unable to import 'transformers'
- **原代码**:
  ```python
  from transformers import LlamaTokenizer
  ```

**8. [PL-possibly-used-before-assignment] 第 148 行**
- **问题**: Possibly using variable 'tokenizer_func' before assignment
- **原代码**:
  ```python
  tokenizer_func,
  ```

**9. [PL-possibly-used-before-assignment] 第 149 行**
- **问题**: Possibly using variable 'eos_token' before assignment
- **原代码**:
  ```python
  eos_token,
  ```

#### 📄 `utils.py`

**1. [PY100] 第 247 行**
- **问题**: 使用了未定义的名称 'extract_problem_statement_and_hints_django'。
- **原代码**:
  ```python
  return extract_problem_statement_and_hints_django(pull, repo)
  ```

**2. [PY100] 第 263 行**
- **问题**: 使用了未定义的名称 '_extract_hints'。
- **原代码**:
  ```python
  hint_texts = _extract_hints(pull, repo, issue_number)
  ```

**3. [PY100] 第 328 行**
- **问题**: 使用了未定义的名称 'test_word'。
- **原代码**:
  ```python
  test_word in hunk.path for test_word in ["test", "tests", "e2e", "testing"]
  ```

**4. [PY100] 第 27 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__(message)
  ```

**5. [PY100] 第 36 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  f"{self.instance_id}: {super().__str__()}\n"
  ```

**6. [PY100] 第 44 行**
- **问题**: 使用了未定义的名称 'load_swebench_dataset'。
- **原代码**:
  ```python
  dataset = load_swebench_dataset(dataset_name, split)
  ```

**7. [PY100] 第 93 行**
- **问题**: 使用了未定义的名称 'run_sequential'。
- **原代码**:
  ```python
  return run_sequential(func, payloads)
  ```

**8. [PY100] 第 208 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  first_chars = list(map(lambda x: None if not len(x) else x[0], hunk.split("\n")))
  ```

**9. [PY100] 第 208 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  first_chars = list(map(lambda x: None if not len(x) else x[0], hunk.split("\n")))
  ```

**10. [PY100] 第 211 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  new_lines = list(map(lambda x: x.rstrip(), hunk.split("\n")[first_idx:last_idx]))
  ```

**11. [PY100] 第 256 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  map(lambda x: int(x) if x.isnumeric() else x, hunk)
  ```

**12. [PY100] 第 256 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  map(lambda x: int(x) if x.isnumeric() else x, hunk)
  ```

**13. [PY100] 第 256 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  map(lambda x: int(x) if x.isnumeric() else x, hunk)
  ```

**14. [PY100] 第 363 行**
- **问题**: 使用了未定义的名称 'swebench'。
- **原代码**:
  ```python
  resources.files(swebench.resources)
  ```

**15. [PY100] 第 37 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  first_chars = list(map(lambda x: None if not len(x) else x[0], hunk.split("\n")))
  ```

**16. [PY100] 第 37 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  first_chars = list(map(lambda x: None if not len(x) else x[0], hunk.split("\n")))
  ```

**17. [PY100] 第 40 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  new_lines = list(map(lambda x: x.rstrip(), hunk.split("\n")[first_idx:last_idx]))
  ```

**18. [PY100] 第 81 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  *list(map(lambda x: int(x) if x.isnumeric() else x, hunk)), total_delta
  ```

**19. [PY100] 第 81 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  *list(map(lambda x: int(x) if x.isnumeric() else x, hunk)), total_delta
  ```

**20. [PY100] 第 81 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  *list(map(lambda x: int(x) if x.isnumeric() else x, hunk)), total_delta
  ```

**21. [PY100] 第 101 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  map(lambda x: int(x) if x.isnumeric() else x, hunk)
  ```

**22. [PY100] 第 101 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  map(lambda x: int(x) if x.isnumeric() else x, hunk)
  ```

**23. [PY100] 第 101 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  map(lambda x: int(x) if x.isnumeric() else x, hunk)
  ```

**24. [PY100] 第 145 行**
- **问题**: 使用了未定义的名称 'word'。
- **原代码**:
  ```python
  return any(word in words for word in test_phrases)
  ```

**25. [PY003] 第 159 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(cmd, shell=True, check=True)
  ```

**26. [PY003] 第 161 行**
- **问题**: subprocess.*(shell=True) 可能导致命令注入。
- **原代码**:
  ```python
  subprocess.run(
  ```

**27. [PY100] 第 171 行**
- **问题**: 使用了未定义的名称 'NotImplementedError'。
- **原代码**:
  ```python
  raise NotImplementedError()  # TODO: activate conda environment and return the environment file
  ```

**28. [PY100] 第 175 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = list(filter(lambda x: os.path.isfile(x), files))
  ```

**29. [PY100] 第 176 行**
- **问题**: 使用了未定义的名称 'x'。
- **原代码**:
  ```python
  files = list(filter(lambda x: x.lower().startswith("readme"), files))
  ```

**30. [PY100] 第 204 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  super().__init__(repo_dir, instance["base_commit"], verbose=verbose)
  ```

**31. [PY100] 第 210 行**
- **问题**: 使用了未定义的名称 'super'。
- **原代码**:
  ```python
  return super().__exit__(exc_type, exc_val, exc_tb)
  ```

**32. [PY100] 第 288 行**
- **问题**: 使用了未定义的名称 'UnicodeDecodeError'。
- **原代码**:
  ```python
  except (UnicodeDecodeError, LookupError):
  ```

**33. [PY100] 第 288 行**
- **问题**: 使用了未定义的名称 'LookupError'。
- **原代码**:
  ```python
  except (UnicodeDecodeError, LookupError):
  ```

### 🟡 MEDIUM 级别 (485 个)

#### 📄 `bm25_retrieval.py`

**1. [AST002] 第 364 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if results is None:
  ```
- **修复方案**:
  ```python
  if results == None:
  ```

**2. [AST002] 第 447 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if index_path is None:
  ```
- **修复方案**:
  ```python
  if index_path == None:
  ```

**3. [AST002] 第 478 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None:
  ```

**4. [PL-logging-fstring-interpolation] 第 59 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Failed to switch to {self.base_commit}")
  ```

**5. [PL-unnecessary-lambda] 第 66 行**
- **问题**: Lambda may not be necessary
- **原代码**:
  ```python
  files = list(filter(lambda x: os.path.isfile(x), files))
  ```

**6. [PL-unspecified-encoding] 第 76 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**7. [PL-broad-exception-caught] 第 96 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**8. [PL-unspecified-encoding] 第 84 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**9. [PL-logging-fstring-interpolation] 第 98 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Failed to parse file {str(filename)}. Using simple filecontent.")
  ```

**10. [PL-unspecified-encoding] 第 99 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**11. [PL-unspecified-encoding] 第 106 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**12. [PL-broad-exception-caught] 第 138 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**13. [PL-bare-except] 第 136 行**
- **问题**: No exception type(s) specified
- **原代码**:
  ```python
  except:
  ```

**14. [PL-logging-fstring-interpolation] 第 140 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Failed to parse file {str(filename)}. Using simple filecontent.")
  ```

**15. [PL-unused-variable] 第 115 行**
- **问题**: Unused variable 'abspath'
- **原代码**:
  ```python
  abspath = Path(filename).absolute()
  ```

**16. [PL-logging-fstring-interpolation] 第 169 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Cloning {repo} {os.getpid()}")
  ```

**17. [PL-unspecified-encoding] 第 228 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(documents_path, "w") as docfile:
  ```
- **修复方案**:
  ```python
  with open(documents_path, "w", encoding="utf-8") as docfile:
  ```

**18. [PL-raise-missing-from] 第 263 行**
- **问题**: Consider explicitly re-raising using 'except KeyboardInterrupt as exc' and 'raise KeyboardInterrupt from exc'
- **原代码**:
  ```python
  raise KeyboardInterrupt
  ```

**19. [PL-logging-not-lazy] 第 265 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(thread_prefix + "Process killed by user")
  ```

**20. [PL-logging-fstring-interpolation] 第 268 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"return code: {proc.returncode}")
  ```

**21. [PL-broad-exception-raised] 第 269 行**
- **问题**: Raising too general exception: Exception
- **原代码**:
  ```python
  raise Exception(
  ```

**22. [PL-unused-argument] 第 199 行**
- **问题**: Unused argument 'query'
- **原代码**:
  ```python
  query,
  ```

**23. [PL-unused-variable] 第 260 行**
- **问题**: Unused variable 'output'
- **原代码**:
  ```python
  output, error = proc.communicate()
  ```

**24. [PL-unspecified-encoding] 第 291 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file) as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, encoding="utf-8") as f:
  ```

**25. [PL-logging-fstring-interpolation] 第 296 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(
  ```

**26. [PL-broad-exception-caught] 第 343 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception:
  ```

**27. [PL-broad-exception-caught] 第 332 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**28. [PL-logging-fstring-interpolation] 第 344 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Failed to process {instance_id}")
  ```

**29. [PL-unspecified-encoding] 第 367 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file, "a") as out_file:
  ```
- **修复方案**:
  ```python
  with open(output_file, "a", encoding="utf-8") as out_file:
  ```

**30. [PL-unspecified-encoding] 第 372 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file) as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, encoding="utf-8") as f:
  ```

**31. [PL-bare-except] 第 409 行**
- **问题**: No exception type(s) specified
- **原代码**:
  ```python
  except:
  ```

**32. [PL-logging-fstring-interpolation] 第 410 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Failed to process {repo}/{commit} (instance {instance_id})")
  ```

**33. [PL-unused-argument] 第 421 行**
- **问题**: Unused argument 'output_file'
- **原代码**:
  ```python
  output_file: str,
  ```

**34. [PL-subprocess-run-check] 第 486 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  python = subprocess.run("which python", shell=True, capture_output=True)
  ```

**35. [PL-logging-fstring-interpolation] 第 505 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Cleaning up {root_dir}")
  ```

**36. [PL-logging-fstring-interpolation] 第 512 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Finished indexing {len(all_index_paths)} instances")
  ```

**37. [PL-logging-fstring-interpolation] 第 515 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"Missing indexes for {len(missing_ids)} instances.")
  ```

**38. [PL-logging-fstring-interpolation] 第 516 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Saved retrieval results to {output_file}")
  ```

**39. [PL-logging-fstring-interpolation] 第 518 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Cleaning up {root_dir}")
  ```

**40. [B615] 第 476 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dataset = load_dataset(dataset_name_or_path)
  ```

#### 📄 `build_dataset.py`

**1. [AST002] 第 60 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if pull["merged_at"] is None:
  ```
- **修复方案**:
  ```python
  if pull["merged_at"] == None:
  ```

**2. [AST002] 第 76 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["patch"] is None or instance["patch"] == "":
  ```
- **修复方案**:
  ```python
  if instance["patch"] == None or instance["patch"] == "":
  ```

**3. [AST002] 第 78 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["problem_statement"] is None or instance["problem_statement"] == "":
  ```
- **修复方案**:
  ```python
  if instance["problem_statement"] == None or instance["problem_statement"] == "":
  ```

**4. [AST002] 第 92 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["test_patch"] is None or instance["test_patch"].strip() == "":
  ```
- **修复方案**:
  ```python
  if instance["test_patch"] == None or instance["test_patch"].strip() == "":
  ```

**5. [AST002] 第 106 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if token is None:
  ```
- **修复方案**:
  ```python
  if token == None:
  ```

**6. [PL-unspecified-encoding] 第 124 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(all_output) as f:
  ```
- **修复方案**:
  ```python
  with open(all_output, encoding="utf-8") as f:
  ```

**7. [PL-logging-fstring-interpolation] 第 137 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**8. [PL-unspecified-encoding] 第 143 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(all_output, write_mode_all) as all_output:
  ```
- **修复方案**:
  ```python
  with open(all_output, write_mode_all, encoding="utf-8") as all_output:
  ```

**9. [PL-unspecified-encoding] 第 146 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output, write_mode) as output:
  ```
- **修复方案**:
  ```python
  with open(output, write_mode, encoding="utf-8") as output:
  ```

**10. [PL-unspecified-encoding] 第 147 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  for ix, line in enumerate(open(pr_file)):
  ```
- **修复方案**:
  ```python
  for ix, line in enumerate(open(pr_file, encoding="utf-8"), encoding="utf-8"):
  ```

**11. [PL-logging-fstring-interpolation] 第 151 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**12. [PL-logging-fstring-interpolation] 第 182 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**13. [PL-logging-fstring-interpolation] 第 185 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

#### 📄 `build_dataset_ft.py`

**1. [PL-unspecified-encoding] 第 34 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(x) as f:
  ```
- **修复方案**:
  ```python
  with open(x, encoding="utf-8") as f:
  ```

**2. [PL-unspecified-encoding] 第 39 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(destination, "w") as f_out:
  ```
- **修复方案**:
  ```python
  with open(destination, "w", encoding="utf-8") as f_out:
  ```

**3. [PL-unspecified-encoding] 第 44 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(dataset_path) as f:
  ```
- **修复方案**:
  ```python
  with open(dataset_path, encoding="utf-8") as f:
  ```

#### 📄 `c.py`

**1. [PL-unused-argument] 第 8 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_redis(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**2. [PL-unused-argument] 第 35 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_jq(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**3. [PL-unused-argument] 第 57 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_doctest(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**4. [PL-unused-argument] 第 94 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_micropython_test(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**5. [PL-unused-argument] 第 113 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_googletest(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**6. [B314] 第 73 行**
- **问题**: Using xml.etree.ElementTree.fromstring to parse untrusted XML data is known to be vulnerable to XML attacks. Replace xml.etree.ElementTree.fromstring with its defusedxml equivalent function or make sure defusedxml.defuse_stdlib() is called
- **原代码**:
  ```python
  root = ET.fromstring(xml_string)
  ```

#### 📄 `call_make_repo.py`

**1. [PL-subprocess-run-check] 第 9 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  out_make = subprocess.run(
  ```

#### 📄 `create_instance.py`

**1. [AST002] 第 362 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert progress_file is not None, "progress_file is required"
  ```
- **修复方案**:
  ```python
  assert progress_file == not None, "progress_file == required"
  ```

**2. [AST002] 第 383 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_context_len is not None:
  ```
- **修复方案**:
  ```python
  if max_context_len == not None:
  ```

**3. [AST002] 第 384 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert tokenizer_name is not None, (
  ```
- **修复方案**:
  ```python
  assert tokenizer_name == not None, (
  ```

**4. [AST002] 第 419 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_context_len is not None:
  ```
- **修复方案**:
  ```python
  if max_context_len == not None:
  ```

**5. [AST002] 第 446 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_context_len is not None:
  ```
- **修复方案**:
  ```python
  if max_context_len == not None:
  ```

**6. [RUFF-W293] 第 31 行**
- **问题**: Blank line contains whitespace

**7. [RUFF-W293] 第 32 行**
- **问题**: Blank line contains whitespace

**8. [RUFF-W293] 第 43 行**
- **问题**: Blank line contains whitespace

**9. [RUFF-W293] 第 72 行**
- **问题**: Blank line contains whitespace

**10. [PL-unused-argument] 第 139 行**
- **问题**: Unused argument 'add_line_numbers'
- **原代码**:
  ```python
  def make_code_text_edits_only(files_dict, patch, add_line_numbers=True):
  ```

**11. [PL-unspecified-encoding] 第 290 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**12. [PL-unspecified-encoding] 第 312 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  retrieval_results = [json.loads(line) for line in open(retrieval_results_path)]
  ```
- **修复方案**:
  ```python
  retrieval_results = [json.loads(line, encoding="utf-8") for line in open(retrieval_results_path, encoding="utf-8")]
  ```

**13. [PL-logging-fstring-interpolation] 第 322 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"Instance {instance_id} not found in retrieval results")
  ```

**14. [PL-unused-argument] 第 304 行**
- **问题**: Unused argument 'file_source'
- **原代码**:
  ```python
  def add_retrieval_results(input_instances, retrieval_file, k, file_source):
  ```

**15. [PL-unspecified-encoding] 第 373 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(progress_file) as f:
  ```
- **修复方案**:
  ```python
  with open(progress_file, encoding="utf-8") as f:
  ```

**16. [PL-logging-fstring-interpolation] 第 377 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found {len(processed_ids)} already processed instances")
  ```

**17. [PL-unspecified-encoding] 第 378 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  progress_file_handle = open(progress_file, "a")
  ```
- **修复方案**:
  ```python
  progress_file_handle = open(progress_file, "a", encoding="utf-8")
  ```

**18. [PL-unspecified-encoding] 第 380 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  progress_file_handle = open(progress_file, "w")
  ```
- **修复方案**:
  ```python
  progress_file_handle = open(progress_file, "w", encoding="utf-8")
  ```

**19. [PL-logging-fstring-interpolation] 第 398 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Processing {len(instances_to_process)} instances")
  ```

**20. [PL-broad-exception-caught] 第 484 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**21. [B108] 第 402 行**
- **问题**: Probable insecure usage of temp file/directory.
- **原代码**:
  ```python
  dir="/scratch" if os.path.exists("/scratch") else "/tmp"
  ```

#### 📄 `create_text_dataset.py`

**1. [AST002] 第 60 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["text_inputs"] is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if instance["text_inputs"] == None or instance["patch"] == None:
  ```

**2. [AST002] 第 60 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["text_inputs"] is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if instance["text_inputs"] == None or instance["patch"] == None:
  ```

**3. [AST002] 第 64 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if text_inputs is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if text_inputs == None or instance["patch"] == None:
  ```

**4. [AST002] 第 64 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if text_inputs is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if text_inputs == None or instance["patch"] == None:
  ```

**5. [AST002] 第 75 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if push_to_hub_user is not None:
  ```
- **修复方案**:
  ```python
  if push_to_hub_user == not None:
  ```

**6. [AST002] 第 77 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert hub_token is not None, (
  ```
- **修复方案**:
  ```python
  assert hub_token == not None, (
  ```

**7. [AST002] 第 80 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert output_dir is None, "Cannot provide output_dir if pushing to the Hub"
  ```
- **修复方案**:
  ```python
  assert output_dir == None, "Cannot provide output_dir if pushing to the Hub"
  ```

**8. [AST002] 第 81 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_context_len is not None:
  ```
- **修复方案**:
  ```python
  if max_context_len == not None:
  ```

**9. [AST002] 第 82 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert tokenizer_name is not None
  ```
- **修复方案**:
  ```python
  assert tokenizer_name == not None
  ```

**10. [AST002] 第 83 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if push_to_hub_user is None and not Path(output_dir).exists():
  ```
- **修复方案**:
  ```python
  if push_to_hub_user == None and not Path(output_dir).exists():
  ```

**11. [AST002] 第 85 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_context_len is not None:
  ```
- **修复方案**:
  ```python
  if max_context_len == not None:
  ```

**12. [AST002] 第 89 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert tokenizer_name is not None, (
  ```
- **修复方案**:
  ```python
  assert tokenizer_name == not None, (
  ```

**13. [AST002] 第 92 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if k is not None:
  ```
- **修复方案**:
  ```python
  if k == not None:
  ```

**14. [AST002] 第 96 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return hub_token if push_to_hub_user is not None else None
  ```
- **修复方案**:
  ```python
  return hub_token if push_to_hub_user == not None else None
  ```

**15. [AST002] 第 107 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if k is not None:
  ```
- **修复方案**:
  ```python
  if k == not None:
  ```

**16. [AST002] 第 109 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_context_len is not None:
  ```
- **修复方案**:
  ```python
  if max_context_len == not None:
  ```

**17. [AST002] 第 140 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if push_to_hub_user is None:
  ```
- **修复方案**:
  ```python
  if push_to_hub_user == None:
  ```

**18. [AST002] 第 241 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if push_to_hub_user is not None:
  ```
- **修复方案**:
  ```python
  if push_to_hub_user == not None:
  ```

**19. [PL-unspecified-encoding] 第 29 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**20. [PL-unspecified-encoding] 第 32 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**21. [PL-logging-fstring-interpolation] 第 53 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found {len(train_instances)} training ids")
  ```

**22. [PL-logging-fstring-interpolation] 第 54 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found {len(eval_instances)} eval ids")
  ```

**23. [PL-logging-fstring-interpolation] 第 61 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"No text for {instance_id}")
  ```

**24. [PL-logging-fstring-interpolation] 第 65 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"No inputs for {instance_id}")
  ```

**25. [PL-logging-fstring-interpolation] 第 146 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**26. [PL-logging-fstring-interpolation] 第 158 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found {set(dataset.keys())} splits")
  ```

**27. [PL-logging-fstring-interpolation] 第 183 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Processing {split} split")
  ```

**28. [PL-unspecified-encoding] 第 210 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(progress_files[split]) as f:
  ```
- **修复方案**:
  ```python
  with open(progress_files[split], encoding="utf-8") as f:
  ```

**29. [PL-logging-fstring-interpolation] 第 222 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(
  ```

**30. [PL-logging-fstring-interpolation] 第 238 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found {len(final_dataset[split])} {split} instances")
  ```

**31. [PL-logging-fstring-interpolation] 第 253 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Finished saving to {output_file}")
  ```

**32. [B615] 第 156 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  else load_dataset(dataset_name_or_path)
  ```

#### 📄 `criteria.py`

**1. [AST002] 第 14 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if re.search(pattern_git_commit_hash, text) is not None:
  ```
- **修复方案**:
  ```python
  if re.search(pattern_git_commit_hash, text) == not None:
  ```

**2. [AST002] 第 17 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if re.search(pattern_django_commit_hash, text) is not None:
  ```
- **修复方案**:
  ```python
  if re.search(pattern_django_commit_hash, text) == not None:
  ```

**3. [AST002] 第 74 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return image_regex.search(text) is not None or video_regex.search(text) is not None
  ```
- **修复方案**:
  ```python
  return image_regex.search(text) == not None or video_regex.search(text) == not None
  ```

**4. [AST002] 第 74 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return image_regex.search(text) is not None or video_regex.search(text) is not None
  ```
- **修复方案**:
  ```python
  return image_regex.search(text) == not None or video_regex.search(text) == not None
  ```

**5. [PL-duplicate-string-formatting-argument] 第 30 行**
- **问题**: Duplicate string formatting argument 'pattern_repo', consider passing as named argument
- **原代码**:
  ```python
  pattern_urls = r"(?:https?://(?!{}).+)|(?:www\.(?!{}).+)".format(
  ```

**6. [PL-missing-timeout] 第 101 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  if requests.get(url).status_code == 200:
  ```

**7. [B113] 第 101 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  if requests.get(url).status_code == 200:
  ```

#### 📄 `default.py`

**1. [PL-raise-missing-from] 第 117 行**
- **问题**: Consider explicitly re-raising using 'raise ExecutionTimeoutError(self.render_template(self.config.timeout_template, action=action, output=output)) from e'
- **原代码**:
  ```python
  raise ExecutionTimeoutError(
  ```

**2. [PL-raise-missing-from] 第 121 行**
- **问题**: Consider explicitly re-raising using 'except TimeoutError as exc' and 'raise ExecutionTimeoutError(self.render_template(self.config.timeout_template, action=action, output='')) from exc'
- **原代码**:
  ```python
  raise ExecutionTimeoutError(self.render_template(self.config.timeout_template, action=action, output=""))
  ```

#### 📄 `delete_gh_workflows.py`

**1. [PL-subprocess-run-check] 第 16 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  branches_command = subprocess.run(
  ```

**2. [PL-subprocess-run-check] 第 21 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(
  ```

**3. [PL-subprocess-run-check] 第 33 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(["git", "checkout", branch])
  ```

**4. [PL-subprocess-run-check] 第 39 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(["rm", "-rf", workflows_path])
  ```

**5. [PL-subprocess-run-check] 第 40 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(["git", "add", "-A"])
  ```

**6. [PL-subprocess-run-check] 第 41 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(["git", "commit", "-m", "Remove .github/workflows folder"])
  ```

**7. [PL-subprocess-run-check] 第 42 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(["git", "push"])
  ```

**8. [PL-subprocess-run-check] 第 47 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(["rm", "-rf", "temp_repo"])
  ```

#### 📄 `distributed_attention.py`

**1. [PL-redefined-builtin] 第 18 行**
- **问题**: Redefining built-in 'input'
- **原代码**:
  ```python
  ctx: Any, input: Tensor, scatter_idx: int, gather_idx: int, group: Any
  ```

#### 📄 `docker_build.py`

**1. [AST002] 第 419 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if logger is None:
  ```
- **修复方案**:
  ```python
  if logger == None:
  ```

**2. [PL-logging-fstring-interpolation] 第 99 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**3. [PL-logging-fstring-interpolation] 第 106 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"[SETUP SCRIPT] {setup_script_name}:\n{setup_script}")
  ```

**4. [PL-unspecified-encoding] 第 111 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(setup_script_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(setup_script_path, "w", encoding="utf-8") as f:
  ```

**5. [PL-logging-fstring-interpolation] 第 114 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(
  ```

**6. [PL-unspecified-encoding] 第 120 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(dockerfile_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(dockerfile_path, "w", encoding="utf-8") as f:
  ```

**7. [PL-logging-fstring-interpolation] 第 124 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**8. [PL-logging-fstring-interpolation] 第 147 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Error: {ansi_escape(chunk['errorDetail']['message'])}")
  ```

**9. [PL-logging-fstring-interpolation] 第 153 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"docker.errors.BuildError during {image_name}: {e}")
  ```

**10. [PL-logging-fstring-interpolation] 第 156 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Error building image {image_name}: {e}")
  ```

**11. [PL-raise-missing-from] 第 248 行**
- **问题**: Consider explicitly re-raising using 'except Exception as exc' and 'raise Exception(f'Base image {test_spec.base_image_key} not found for {test_spec.env_image_key}\n.Please build the base images first.') from exc'
- **原代码**:
  ```python
  raise Exception(
  ```

**12. [PL-broad-exception-raised] 第 248 行**
- **问题**: Raising too general exception: Exception
- **原代码**:
  ```python
  raise Exception(
  ```

**13. [PL-unused-variable] 第 246 行**
- **问题**: Unused variable 'base_image'
- **原代码**:
  ```python
  base_image = base_images[test_spec.base_image_key]
  ```

**14. [PL-unused-variable] 第 256 行**
- **问题**: Unused variable 'env_image'
- **原代码**:
  ```python
  env_image = client.images.get(test_spec.env_image_key)
  ```

**15. [PL-logging-fstring-interpolation] 第 437 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**16. [PL-logging-fstring-interpolation] 第 464 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Image {image_name} already exists, skipping build.")
  ```

**17. [PL-unused-variable] 第 430 行**
- **问题**: Unused variable 'env_image'
- **原代码**:
  ```python
  env_image = client.images.get(env_image_name)
  ```

**18. [PL-raise-missing-from] 第 503 行**
- **问题**: Consider explicitly re-raising using 'raise Exception(f'Error occurred while pulling image {test_spec.base_image_key}: {str(e)}') from e'
- **原代码**:
  ```python
  raise Exception(
  ```

**19. [PL-broad-exception-raised] 第 503 行**
- **问题**: Raising too general exception: Exception
- **原代码**:
  ```python
  raise Exception(
  ```

#### 📄 `docker_utils.py`

**1. [AST002] 第 212 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if exec_id is not None:
  ```
- **修复方案**:
  ```python
  if exec_id == not None:
  ```

**2. [PL-broad-exception-caught] 第 94 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PL-broad-exception-caught] 第 136 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**4. [PL-broad-exception-caught] 第 153 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e2:
  ```

**5. [PL-broad-exception-caught] 第 166 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**6. [PL-broad-exception-caught] 第 198 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**7. [PL-broad-exception-caught] 第 289 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

#### 📄 `eval_retrieval.py`

**1. [PL-bare-except] 第 21 行**
- **问题**: No exception type(s) specified
- **原代码**:
  ```python
  except:
  ```

**2. [B615] 第 22 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dataset = load_dataset(dataset_name_or_path, split=split)
  ```

#### 📄 `get_tasks_pipeline.py`

**1. [PL-broad-exception-caught] 第 89 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PL-broad-exception-raised] 第 120 行**
- **问题**: Raising too general exception: Exception
- **原代码**:
  ```python
  raise Exception(
  ```

#### 📄 `get_top_pypi.py`

**1. [AST002] 第 43 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if content is not None and package_url in content:
  ```
- **修复方案**:
  ```python
  if content == not None and package_url in content:
  ```

**2. [AST002] 第 65 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if package_github is not None:
  ```
- **修复方案**:
  ```python
  if package_github == not None:
  ```

**3. [PL-redefined-outer-name] 第 49 行**
- **问题**: Redefining name 'soup' from outer scope (line 109)
- **原代码**:
  ```python
  soup = BeautifulSoup(driver.page_source, "html.parser")
  ```

**4. [PL-unspecified-encoding] 第 32 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(f) as fp_:
  ```
- **修复方案**:
  ```python
  with open(f, encoding="utf-8") as fp_:
  ```

**5. [PL-unspecified-encoding] 第 38 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(f, access_type) as fp_:
  ```
- **修复方案**:
  ```python
  with open(f, access_type, encoding="utf-8") as fp_:
  ```

**6. [PL-bare-except] 第 74 行**
- **问题**: No exception type(s) specified
- **原代码**:
  ```python
  except:
  ```

#### 📄 `get_versions.py`

**1. [AST002] 第 48 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if matches is not None:
  ```
- **修复方案**:
  ```python
  if matches == not None:
  ```

**2. [AST002] 第 81 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if is_build and path_repo is not None:
  ```
- **修复方案**:
  ```python
  if is_build and path_repo == not None:
  ```

**3. [AST002] 第 96 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if version is not None:
  ```
- **修复方案**:
  ```python
  if version == not None:
  ```

**4. [AST002] 第 210 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if version is not None:
  ```
- **修复方案**:
  ```python
  if version == not None:
  ```

**5. [AST002] 第 213 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif version_not_found is not None:
  ```
- **修复方案**:
  ```python
  elif version_not_found == not None:
  ```

**6. [AST002] 第 242 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if output_dir is not None:
  ```
- **修复方案**:
  ```python
  if output_dir == not None:
  ```

**7. [PL-logging-fstring-interpolation] 第 84 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found version file at {path_to_version}")
  ```

**8. [PL-unspecified-encoding] 第 85 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(path_to_version) as f:
  ```
- **修复方案**:
  ```python
  with open(path_to_version, encoding="utf-8") as f:
  ```

**9. [PL-missing-timeout] 第 94 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  init_text = requests.get(url).text
  ```

**10. [PL-subprocess-run-check] 第 167 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  out_check = subprocess.run(
  ```

**11. [PL-logging-fstring-interpolation] 第 173 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"[{instance['instance_id']}] Checkout failed")
  ```

**12. [PL-subprocess-run-check] 第 177 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  out_install = subprocess.run(
  ```

**13. [PL-logging-fstring-interpolation] 第 183 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"[{instance['instance_id']}] Installation failed")
  ```

**14. [PL-logging-fstring-interpolation] 第 189 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"For instance {instance['instance_id']}, version is {version}")
  ```

**15. [PL-unspecified-encoding] 第 192 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(save_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(save_path, "w", encoding="utf-8") as f:
  ```

**16. [PL-logging-fstring-interpolation] 第 212 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"For instance {instance['instance_id']}, version is {version}")
  ```

**17. [PL-logging-fstring-interpolation] 第 214 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"[{instance['instance_id']}]: version not found")
  ```

**18. [PL-unspecified-encoding] 第 216 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(save_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(save_path, "w", encoding="utf-8") as f:
  ```

**19. [PL-unspecified-encoding] 第 234 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(task_with_version_path) as f:
  ```
- **修复方案**:
  ```python
  with open(task_with_version_path, encoding="utf-8") as f:
  ```

**20. [PL-unspecified-encoding] 第 244 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(f"{instances_path_new}", "w") as f:
  ```
- **修复方案**:
  ```python
  with open(f"{instances_path_new}", "w", encoding="utf-8") as f:
  ```

**21. [PL-logging-fstring-interpolation] 第 246 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**22. [PL-redefined-outer-name] 第 252 行**
- **问题**: Redefining name 'args' from outer scope (line 426)
- **原代码**:
  ```python
  def main(args):
  ```

**23. [PL-logging-fstring-interpolation] 第 261 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**24. [PL-logging-fstring-interpolation] 第 264 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**25. [PL-logging-fstring-interpolation] 第 301 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Retrieved {total_web} versions from web")
  ```

**26. [PL-logging-fstring-interpolation] 第 303 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**27. [PL-logging-fstring-interpolation] 第 318 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**28. [PL-logging-fstring-interpolation] 第 326 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**29. [PL-logging-fstring-interpolation] 第 332 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Creating clone of {args.conda_env} at {conda_env_name}")
  ```

**30. [PL-logging-fstring-interpolation] 第 338 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**31. [B113] 第 94 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  init_text = requests.get(url).text
  ```

#### 📄 `get_versions_astropy.py`

**1. [PL-missing-timeout] 第 18 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  resp = requests.get("https://docs.astropy.org/en/latest/changelog.html")
  ```

**2. [PL-undefined-loop-variable] 第 66 行**
- **问题**: Using possibly undefined loop variable 't'
- **原代码**:
  ```python
  map_v_to_t[task["version"]].append(t)
  ```

**3. [PL-unspecified-encoding] 第 69 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(
  ```
- **修复方案**:
  ```python
  with open(
  ```

**4. [B113] 第 18 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  resp = requests.get("https://docs.astropy.org/en/latest/changelog.html")
  ```

#### 📄 `get_versions_matplotlib.py`

**1. [PL-missing-timeout] 第 18 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  resp = requests.get("https://matplotlib.org/stable/users/release_notes#past-versions")
  ```

**2. [PL-unspecified-encoding] 第 53 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(
  ```
- **修复方案**:
  ```python
  with open(
  ```

**3. [B113] 第 18 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  resp = requests.get("https://matplotlib.org/stable/users/release_notes#past-versions")
  ```

#### 📄 `get_versions_pvlib-python.py`

**1. [PL-missing-timeout] 第 23 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  resp = requests.get(WEBPAGE)
  ```

**2. [PL-undefined-loop-variable] 第 67 行**
- **问题**: Using possibly undefined loop variable 't'
- **原代码**:
  ```python
  map_v_to_t[task["version"]].append(t)
  ```

**3. [PL-unspecified-encoding] 第 70 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(PATH_TASKS_PVLIB_V, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(PATH_TASKS_PVLIB_V, "w", encoding="utf-8") as f:
  ```

**4. [B113] 第 23 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  resp = requests.get(WEBPAGE)
  ```

#### 📄 `get_versions_pydicom.py`

**1. [PL-missing-timeout] 第 14 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  resp = requests.get("https://pydicom.github.io/pydicom/dev/faq/index.html")
  ```

**2. [PL-unspecified-encoding] 第 43 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(PATH_TASKS_PYDICOM_V, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(PATH_TASKS_PYDICOM_V, "w", encoding="utf-8") as f:
  ```

**3. [B113] 第 14 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  resp = requests.get("https://pydicom.github.io/pydicom/dev/faq/index.html")
  ```

#### 📄 `get_versions_sqlfluff.py`

**1. [AST002] 第 93 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  print(sorted(list({t["version"] for t in versioned if t["version"] is not None})))
  ```
- **修复方案**:
  ```python
  print(sorted(list({t["version"] for t in versioned if t["version"] == not None})))
  ```

**2. [PL-redefined-outer-name] 第 38 行**
- **问题**: Redefining name 'version' from outer scope (line 58)
- **原代码**:
  ```python
  version = parts[0].replace("[", "").replace("]", "")
  ```

**3. [PL-unspecified-encoding] 第 85 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(
  ```
- **修复方案**:
  ```python
  with open(
  ```

**4. [PL-unspecified-encoding] 第 92 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  versioned = json.load(open(os.path.join(PATH_TO_SAVE, versioned_path)))
  ```
- **修复方案**:
  ```python
  versioned = json.load(open(os.path.join(PATH_TO_SAVE, versioned_path, encoding="utf-8"), encoding="utf-8"), encoding="utf-8")
  ```

#### 📄 `get_versions_xarray.py`

**1. [PL-missing-timeout] 第 18 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  resp = requests.get("https://docs.xarray.dev/en/stable/whats-new.html")
  ```

**2. [PL-bare-except] 第 40 行**
- **问题**: No exception type(s) specified
- **原代码**:
  ```python
  except:
  ```

**3. [PL-unspecified-encoding] 第 58 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(
  ```
- **修复方案**:
  ```python
  with open(
  ```

**4. [B113] 第 18 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  resp = requests.get("https://docs.xarray.dev/en/stable/whats-new.html")
  ```

#### 📄 `go.py`

**1. [PL-unused-argument] 第 6 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_gotest(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

#### 📄 `grading.py`

**1. [AST002] 第 264 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if prediction[KEY_PREDICTION] is None:
  ```
- **修复方案**:
  ```python
  if prediction[KEY_PREDICTION] == None:
  ```

**2. [PL-fixme] 第 60 行**
- **问题**: TODO fix constant here
- **原代码**:
  ```python
  # TODO fixers constant here
  ```

**3. [PL-fixme] 第 210 行**
- **问题**: TODO: Don't factor in p2p metrics
- **原代码**:
  ```python
  # TODO: Don't factor in p2p metrics
  ```

**4. [PL-unspecified-encoding] 第 58 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(log_fp) as f:
  ```
- **修复方案**:
  ```python
  with open(log_fp, encoding="utf-8") as f:
  ```

#### 📄 `java.py`

**1. [PL-unused-argument] 第 6 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_maven(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**2. [PL-unused-argument] 第 42 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_ant(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**3. [PL-unused-argument] 第 59 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_gradle_custom(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

#### 📄 `javascript.py`

**1. [AST002] 第 27 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if re.search(r"\(\d+ms\)", line) is not None
  ```
- **修复方案**:
  ```python
  if re.search(r"\(\d+ms\)", line) == not None
  ```

**2. [AST002] 第 37 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if re.search(r"\(\d+ms\)", line) is not None
  ```
- **修复方案**:
  ```python
  if re.search(r"\(\d+ms\)", line) == not None
  ```

#### 📄 `make_lite.py`

**1. [PL-redefined-outer-name] 第 49 行**
- **问题**: Redefining name 'dset' from outer scope (line 85)
- **原代码**:
  ```python
  def apply_filters(dset, filters, name=""):
  ```

**2. [PL-redefined-outer-name] 第 61 行**
- **问题**: Redefining name 'dset' from outer scope (line 85)
- **原代码**:
  ```python
  def take_subset(dset, n, name=""):
  ```

**3. [B615] 第 75 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dev = load_dataset("SWE-bench/SWE-bench")["dev"]
  ```

**4. [B615] 第 76 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  test = load_dataset("SWE-bench/SWE-bench")["test"]
  ```

#### 📄 `modeling_flash_llama.py`

**1. [AST002] 第 139 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if scale_base is not None
  ```
- **修复方案**:
  ```python
  if scale_base == not None
  ```

**2. [AST002] 第 191 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if self.scale is None:
  ```
- **修复方案**:
  ```python
  if self.scale == None:
  ```

**3. [AST002] 第 221 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if unpadded_lengths is not None:
  ```
- **修复方案**:
  ```python
  if unpadded_lengths == not None:
  ```

**4. [AST002] 第 229 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if self.scale is None:
  ```
- **修复方案**:
  ```python
  if self.scale == None:
  ```

**5. [AST002] 第 360 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  has_layer_past = past_key_value is not None and past_key_value[0] is not None
  ```
- **修复方案**:
  ```python
  has_layer_past = past_key_value == not None and past_key_value[0] == not None
  ```

**6. [AST002] 第 360 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  has_layer_past = past_key_value is not None and past_key_value[0] is not None
  ```
- **修复方案**:
  ```python
  has_layer_past = past_key_value == not None and past_key_value[0] == not None
  ```

**7. [AST002] 第 369 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if position_ids is not None:
  ```
- **修复方案**:
  ```python
  if position_ids == not None:
  ```

**8. [AST002] 第 413 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert output_attentions is False
  ```
- **修复方案**:
  ```python
  assert output_attentions == False
  ```

**9. [AST002] 第 424 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if unpadded_lengths is not None:
  ```
- **修复方案**:
  ```python
  if unpadded_lengths == not None:
  ```

**10. [AST002] 第 426 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert attention_mask is not None
  ```
- **修复方案**:
  ```python
  assert attention_mask == not None
  ```

**11. [AST002] 第 547 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if module.bias is not None:
  ```
- **修复方案**:
  ```python
  if module.bias == not None:
  ```

**12. [AST002] 第 551 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if module.padding_idx is not None:
  ```
- **修复方案**:
  ```python
  if module.padding_idx == not None:
  ```

**13. [AST002] 第 605 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if output_attentions is not None
  ```
- **修复方案**:
  ```python
  if output_attentions == not None
  ```

**14. [AST002] 第 610 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if output_hidden_states is not None
  ```
- **修复方案**:
  ```python
  if output_hidden_states == not None
  ```

**15. [AST002] 第 613 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  use_cache = use_cache if use_cache is not None else self.config.use_cache
  ```
- **修复方案**:
  ```python
  use_cache = use_cache if use_cache == not None else self.config.use_cache
  ```

**16. [AST002] 第 616 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return_dict if return_dict is not None else self.config.use_return_dict
  ```
- **修复方案**:
  ```python
  return_dict if return_dict == not None else self.config.use_return_dict
  ```

**17. [AST002] 第 620 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if input_ids is not None and inputs_embeds is not None:
  ```
- **修复方案**:
  ```python
  if input_ids == not None and inputs_embeds == not None:
  ```

**18. [AST002] 第 620 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if input_ids is not None and inputs_embeds is not None:
  ```
- **修复方案**:
  ```python
  if input_ids == not None and inputs_embeds == not None:
  ```

**19. [AST002] 第 624 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif input_ids is not None:
  ```
- **修复方案**:
  ```python
  elif input_ids == not None:
  ```

**20. [AST002] 第 626 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif inputs_embeds is not None:
  ```
- **修复方案**:
  ```python
  elif inputs_embeds == not None:
  ```

**21. [AST002] 第 635 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if inputs_embeds is None:
  ```
- **修复方案**:
  ```python
  if inputs_embeds == None:
  ```

**22. [AST002] 第 649 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  ((attention_mask is not None) and (not attention_mask.all().item()))
  ```
- **修复方案**:
  ```python
  ((attention_mask == not None) and (not attention_mask.all().item()))
  ```

**23. [AST002] 第 669 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if unpadded_lengths is not None:
  ```
- **修复方案**:
  ```python
  if unpadded_lengths == not None:
  ```

**24. [AST002] 第 678 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if past_key_values is not None and idx < len(past_key_values)
  ```
- **修复方案**:
  ```python
  if past_key_values == not None and idx < len(past_key_values)
  ```

**25. [AST002] 第 714 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if unpadded_lengths is not None:
  ```
- **修复方案**:
  ```python
  if unpadded_lengths == not None:
  ```

**26. [AST002] 第 727 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if v is not None
  ```
- **修复方案**:
  ```python
  if v == not None
  ```

**27. [AST002] 第 811 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if output_attentions is not None
  ```
- **修复方案**:
  ```python
  if output_attentions == not None
  ```

**28. [AST002] 第 816 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if output_hidden_states is not None
  ```
- **修复方案**:
  ```python
  if output_hidden_states == not None
  ```

**29. [AST002] 第 820 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return_dict if return_dict is not None else self.config.use_return_dict
  ```
- **修复方案**:
  ```python
  return_dict if return_dict == not None else self.config.use_return_dict
  ```

**30. [AST002] 第 839 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if labels is not None:
  ```
- **修复方案**:
  ```python
  if labels == not None:
  ```

**31. [AST002] 第 849 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  avg_valid_labels_per_chunk is not None
  ```
- **修复方案**:
  ```python
  avg_valid_labels_per_chunk == not None
  ```

**32. [AST002] 第 865 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return (loss,) + output if loss is not None else output
  ```
- **修复方案**:
  ```python
  return (loss,) + output if loss == not None else output
  ```

**33. [AST002] 第 887 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if inputs_embeds is not None and past_key_values is None:
  ```
- **修复方案**:
  ```python
  if inputs_embeds == not None and past_key_values == None:
  ```

**34. [AST002] 第 887 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if inputs_embeds is not None and past_key_values is None:
  ```
- **修复方案**:
  ```python
  if inputs_embeds == not None and past_key_values == None:
  ```

**35. [AST002] 第 899 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  (attention_mask is not None) and (not attention_mask.all().item())
  ```
- **修复方案**:
  ```python
  (attention_mask == not None) and (not attention_mask.all().item())
  ```

**36. [AST002] 第 955 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return_dict if return_dict is not None else self.config.use_return_dict
  ```
- **修复方案**:
  ```python
  return_dict if return_dict == not None else self.config.use_return_dict
  ```

**37. [AST002] 第 972 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if input_ids is not None:
  ```
- **修复方案**:
  ```python
  if input_ids == not None:
  ```

**38. [AST002] 第 977 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if self.config.pad_token_id is None and batch_size != 1:
  ```
- **修复方案**:
  ```python
  if self.config.pad_token_id == None and batch_size != 1:
  ```

**39. [AST002] 第 981 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if self.config.pad_token_id is None:
  ```
- **修复方案**:
  ```python
  if self.config.pad_token_id == None:
  ```

**40. [AST002] 第 984 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if input_ids is not None:
  ```
- **修复方案**:
  ```python
  if input_ids == not None:
  ```

**41. [AST002] 第 996 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if labels is not None:
  ```
- **修复方案**:
  ```python
  if labels == not None:
  ```

**42. [AST002] 第 998 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if self.config.problem_type is None:
  ```
- **修复方案**:
  ```python
  if self.config.problem_type == None:
  ```

**43. [AST002] 第 1024 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return ((loss,) + output) if loss is not None else output
  ```
- **修复方案**:
  ```python
  return ((loss,) + output) if loss == not None else output
  ```

**44. [PL-raise-missing-from] 第 49 行**
- **问题**: Consider explicitly re-raising using 'except ImportError as exc' and 'raise ImportError('Please install RoPE kernels: `pip install git+https://github.com/HazyResearch/flash-attention.git#subdirectory=csrc/rotary`') from exc'
- **原代码**:
  ```python
  raise ImportError(
  ```

**45. [PL-unused-variable] 第 625 行**
- **问题**: Unused variable 'batch_size'
- **原代码**:
  ```python
  batch_size, seq_length = input_ids.shape
  ```

**46. [PL-unused-variable] 第 625 行**
- **问题**: Unused variable 'seq_length'
- **原代码**:
  ```python
  batch_size, seq_length = input_ids.shape
  ```

**47. [PL-unused-argument] 第 779 行**
- **问题**: Unused argument 'unpadded_lengths'
- **原代码**:
  ```python
  unpadded_lengths: Optional[bool] = None,
  ```

#### 📄 `php.py`

**1. [PL-unused-argument] 第 6 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_phpunit(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

#### 📄 `prepare_images.py`

**1. [AST002] 第 35 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance_ids is None:
  ```
- **修复方案**:
  ```python
  if instance_ids == None:
  ```

#### 📄 `print_pulls.py`

**1. [AST002] 第 38 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if cutoff_date is not None
  ```
- **修复方案**:
  ```python
  if cutoff_date == not None
  ```

**2. [AST002] 第 46 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_pulls is not None and i_pull >= max_pulls:
  ```
- **修复方案**:
  ```python
  if max_pulls == not None and i_pull >= max_pulls:
  ```

**3. [AST002] 第 48 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if cutoff_date is not None and pull.created_at < cutoff_date:
  ```
- **修复方案**:
  ```python
  if cutoff_date == not None and pull.created_at < cutoff_date:
  ```

**4. [AST002] 第 72 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if pull is None:
  ```
- **修复方案**:
  ```python
  if pull == None:
  ```

**5. [AST002] 第 106 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if token is None:
  ```
- **修复方案**:
  ```python
  if token == None:
  ```

**6. [AST002] 第 111 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if pull_number is not None:
  ```
- **修复方案**:
  ```python
  if pull_number == not None:
  ```

**7. [PL-unspecified-encoding] 第 42 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output, "w") as file:
  ```
- **修复方案**:
  ```python
  with open(output, "w", encoding="utf-8") as file:
  ```

**8. [PL-logging-fstring-interpolation] 第 65 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Fetching PR #{pull_number} from {repo.owner}/{repo.name}")
  ```

**9. [PL-logging-fstring-interpolation] 第 73 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"PR #{pull_number} not found in {repo.owner}/{repo.name}")
  ```

**10. [PL-unspecified-encoding] 第 80 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output, "w") as file:
  ```
- **修复方案**:
  ```python
  with open(output, "w", encoding="utf-8") as file:
  ```

**11. [PL-logging-fstring-interpolation] 第 83 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"PR #{pull_number} saved to {output}")
  ```

**12. [PL-logging-fstring-interpolation] 第 84 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Resolved issues: {pull.resolved_issues}")
  ```

#### 📄 `python.py`

**1. [AST002] 第 119 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if line.lstrip().startswith("ok") and prev_test is not None:
  ```
- **修复方案**:
  ```python
  if line.lstrip().startswith("ok") and prev_test == not None:
  ```

**2. [PL-missing-timeout] 第 35 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  reqs = requests.get(reqs_url, headers=HEADERS)
  ```

**3. [PL-unused-variable] 第 75 行**
- **问题**: Unused variable 'pip_line_start'
- **原代码**:
  ```python
  pip_line_start = pip_match.start()
  ```

**4. [PL-missing-timeout] 第 141 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  reqs = requests.get(reqs_url, headers=HEADERS)
  ```

**5. [PL-missing-timeout] 第 168 行**
- **问题**: Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely
- **原代码**:
  ```python
  reqs = requests.get(reqs_url, headers=HEADERS)
  ```

**6. [PL-unused-argument] 第 321 行**
- **问题**: Unused argument 'instance'
- **原代码**:
  ```python
  instance, specs, env_name, cached_environment_yml
  ```

**7. [PL-unused-argument] 第 321 行**
- **问题**: Unused argument 'specs'
- **原代码**:
  ```python
  instance, specs, env_name, cached_environment_yml
  ```

**8. [B113] 第 35 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  reqs = requests.get(reqs_url, headers=HEADERS)
  ```

**9. [B113] 第 141 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  reqs = requests.get(reqs_url, headers=HEADERS)
  ```

**10. [B113] 第 168 行**
- **问题**: Call to requests without timeout
- **原代码**:
  ```python
  reqs = requests.get(reqs_url, headers=HEADERS)
  ```

#### 📄 `remove_containers.py`

**1. [PL-pointless-string-statement] 第 6 行**
- **问题**: String statement has no effect
- **原代码**:
  ```python
  """
  ```

**2. [PL-redefined-outer-name] 第 11 行**
- **问题**: Redefining name 'instance_ids' from outer scope (line 51)
- **原代码**:
  ```python
  def main(instance_ids, predictions_path):
  ```

**3. [PL-unspecified-encoding] 第 14 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(predictions_path, "r") as f:
  ```
- **修复方案**:
  ```python
  with open(predictions_path, "r", encoding="utf-8") as f:
  ```

**4. [PL-broad-exception-caught] 第 35 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

#### 📄 `remove_envs.py`

**1. [PL-subprocess-run-check] 第 40 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  subprocess.run(command.split(" "))
  ```

**2. [PL-pointless-string-statement] 第 63 行**
- **问题**: String statement has no effect
- **原代码**:
  ```python
  """
  ```

#### 📄 `reporting.py`

**1. [PL-unspecified-encoding] 第 157 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(report_file, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(report_file, "w", encoding="utf-8") as f:
  ```

#### 📄 `ruby.py`

**1. [PL-unused-argument] 第 7 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_minitest(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**2. [PL-unused-argument] 第 30 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_cucumber(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**3. [PL-unused-argument] 第 50 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_ruby_unit(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

**4. [PL-unused-argument] 第 69 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_rspec_transformed_json(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

#### 📄 `run_api.py`

**1. [AST002] 第 200 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if openai_key is None:
  ```
- **修复方案**:
  ```python
  if openai_key == None:
  ```

**2. [AST002] 第 240 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_cost is not None and total_cost >= max_cost:
  ```
- **修复方案**:
  ```python
  if max_cost == not None and total_cost >= max_cost:
  ```

**3. [AST002] 第 343 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if api_key is None:
  ```
- **修复方案**:
  ```python
  if api_key == None:
  ```

**4. [AST002] 第 401 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_cost is not None and total_cost >= max_cost:
  ```
- **修复方案**:
  ```python
  if max_cost == not None and total_cost >= max_cost:
  ```

**5. [AST002] 第 417 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if model_args is not None:
  ```
- **修复方案**:
  ```python
  if model_args == not None:
  ```

**6. [AST002] 第 452 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == None and num_shards == not None:
  ```

**7. [AST002] 第 452 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == None and num_shards == not None:
  ```

**8. [AST002] 第 456 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == None:
  ```

**9. [AST002] 第 456 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == None:
  ```

**10. [AST002] 第 465 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**11. [AST002] 第 465 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**12. [AST002] 第 492 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**13. [AST002] 第 492 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**14. [PL-logging-fstring-interpolation] 第 107 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**15. [PL-unspecified-encoding] 第 219 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file, "a+") as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, "a+", encoding="utf-8") as f:
  ```

**16. [PL-broad-exception-caught] 第 274 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**17. [PL-logging-fstring-interpolation] 第 276 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Inputs: {inputs}")
  ```

**18. [PL-broad-exception-caught] 第 315 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**19. [PL-logging-fstring-interpolation] 第 317 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.error(f"Inputs: {inputs}")
  ```

**20. [PL-unused-argument] 第 284 行**
- **问题**: Unused argument 'model_args'
- **原代码**:
  ```python
  inputs, anthropic, model_name_or_path, temperature, top_p, **model_args
  ```

**21. [PL-unspecified-encoding] 第 367 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file, "a+") as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, "a+", encoding="utf-8") as f:
  ```

**22. [PL-broad-exception-caught] 第 389 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**23. [PL-logging-fstring-interpolation] 第 453 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(
  ```

**24. [PL-logging-fstring-interpolation] 第 457 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"Received shard_id={shard_id} but num_shards is None, ignoring")
  ```

**25. [PL-logging-fstring-interpolation] 第 468 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Will write to {output_file}")
  ```

**26. [PL-unspecified-encoding] 第 471 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file) as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, encoding="utf-8") as f:
  ```

**27. [PL-logging-fstring-interpolation] 第 476 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Read {len(existing_ids)} already completed ids from {output_file}")
  ```

**28. [B615] 第 480 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dataset = load_dataset(dataset_name_or_path)
  ```

#### 📄 `run_evaluation.py`

**1. [AST002] 第 461 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if v[KEY_PREDICTION] == "" or v[KEY_PREDICTION] is None
  ```
- **修复方案**:
  ```python
  if v[KEY_PREDICTION] == "" or v[KEY_PREDICTION] == None
  ```

**2. [AST002] 第 505 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if report_dir is not None:
  ```
- **修复方案**:
  ```python
  if report_dir == not None:
  ```

**3. [AST002] 第 510 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if force_rebuild and namespace is not None:
  ```
- **修复方案**:
  ```python
  if force_rebuild and namespace == not None:
  ```

**4. [AST002] 第 542 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if namespace is None and not rewrite_reports:
  ```
- **修复方案**:
  ```python
  if namespace == None and not rewrite_reports:
  ```

**5. [PL-unspecified-encoding] 第 112 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(report_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(report_path, "w", encoding="utf-8") as f:
  ```

**6. [PL-bare-except] 第 137 行**
- **问题**: No exception type(s) specified
- **原代码**:
  ```python
  except:
  ```

**7. [PL-return-in-finally] 第 270 行**
- **问题**: 'return' shadowed by the 'finally' clause.
- **原代码**:
  ```python
  return {
  ```

**8. [PL-broad-exception-caught] 第 257 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**9. [PL-unspecified-encoding] 第 160 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  patch_file.write_text(pred[KEY_PREDICTION] or "")
  ```

**10. [PL-unspecified-encoding] 第 199 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  eval_file.write_text(test_spec.eval_script)
  ```

**11. [PL-unspecified-encoding] 第 211 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(test_output_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(test_output_path, "w", encoding="utf-8") as f:
  ```

**12. [PL-unspecified-encoding] 第 250 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(report_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(report_path, "w", encoding="utf-8") as f:
  ```

**13. [PL-lost-exception] 第 270 行**
- **问题**: return statement in finally block may swallow exception
- **原代码**:
  ```python
  return {
  ```

**14. [PL-redefined-outer-name] 第 356 行**
- **问题**: Redefining name 'args' from outer scope (line 676)
- **原代码**:
  ```python
  def run_evaluation_with_progress(*args):
  ```

#### 📄 `run_evaluation_modal.py`

**1. [AST002] 第 75 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if timeout is None:
  ```
- **修复方案**:
  ```python
  if timeout == None:
  ```

**2. [PL-broad-exception-caught] 第 98 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PL-broad-exception-caught] 第 134 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**4. [PL-broad-exception-caught] 第 154 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception:
  ```

**5. [PL-broad-exception-caught] 第 150 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception:
  ```

**6. [PL-unspecified-encoding] 第 172 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  Path(remote_env_script_path).write_text(env_script)
  ```

**7. [PL-unspecified-encoding] 第 173 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  Path(remote_repo_script_path).write_text(repo_script)
  ```

**8. [PL-broad-exception-caught] 第 376 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**9. [PL-unspecified-encoding] 第 323 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(test_output_path, "w") as f:
  ```
- **修复方案**:
  ```python
  with open(test_output_path, "w", encoding="utf-8") as f:
  ```

**10. [PL-unspecified-encoding] 第 448 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(log_dir / "run_instance.log", "w") as f:
  ```
- **修复方案**:
  ```python
  with open(log_dir / "run_instance.log", "w", encoding="utf-8") as f:
  ```

**11. [PL-unspecified-encoding] 第 450 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(log_dir / "test_output.txt", "w") as f:
  ```
- **修复方案**:
  ```python
  with open(log_dir / "test_output.txt", "w", encoding="utf-8") as f:
  ```

**12. [PL-unspecified-encoding] 第 452 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(log_dir / "patch.diff", "w") as f:
  ```
- **修复方案**:
  ```python
  with open(log_dir / "patch.diff", "w", encoding="utf-8") as f:
  ```

**13. [PL-unspecified-encoding] 第 454 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(log_dir / "report.json", "w") as f:
  ```
- **修复方案**:
  ```python
  with open(log_dir / "report.json", "w", encoding="utf-8") as f:
  ```

**14. [PL-broad-exception-caught] 第 458 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception:
  ```

**15. [B108] 第 269 行**
- **问题**: Probable insecure usage of temp file/directory.
- **原代码**:
  ```python
  patch_file = "/tmp/patch.diff"
  ```

#### 📄 `run_evaluation_modal_entrypoint.py`

**1. [PL-redefined-builtin] 第 16 行**
- **问题**: Redefining built-in 'exec'
- **原代码**:
  ```python
  async def exec(command: str) -> int:
  ```

**2. [B102] 第 112 行**
- **问题**: Use of exec detected.
- **原代码**:
  ```python
  returncode = await exec(command)
  ```

#### 📄 `run_live.py`

**1. [AST002] 第 111 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if commit is None:
  ```
- **修复方案**:
  ```python
  if commit == None:
  ```

**2. [AST002] 第 184 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if base_commit is not None and len(issue_url) != len(base_commit):
  ```
- **修复方案**:
  ```python
  if base_commit == not None and len(issue_url) != len(base_commit):
  ```

**3. [AST002] 第 188 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if base_commit is None:
  ```
- **修复方案**:
  ```python
  if base_commit == None:
  ```

**4. [AST002] 第 191 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if gh_token is not None:
  ```
- **修复方案**:
  ```python
  if gh_token == not None:
  ```

**5. [PL-logging-fstring-interpolation] 第 109 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Cloning repo {owner}/{repo}")
  ```

**6. [PL-logging-fstring-interpolation] 第 117 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Building BM25 retrieval index for {owner}/{repo}@{commit}")
  ```

**7. [PL-logging-fstring-interpolation] 第 129 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Retrieved {len(hits)} documents")
  ```

**8. [PL-unspecified-encoding] 第 137 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  hit["file_contents"] = open(hit["docid"]).read()
  ```
- **修复方案**:
  ```python
  hit["file_contents"] = open(hit["docid"], encoding="utf-8").read(, encoding="utf-8")
  ```

**9. [PL-logging-not-lazy] 第 150 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**10. [PL-unused-variable] 第 107 行**
- **问题**: Unused variable 'thread_id'
- **原代码**:
  ```python
  thread_id = 0
  ```

**11. [PL-logging-fstring-interpolation] 第 192 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"Using GitHub token: {'*' * 8}{gh_token[-4:]}")
  ```

**12. [PL-logging-fstring-interpolation] 第 202 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Creating instance {instance_id}")
  ```

**13. [PL-logging-fstring-interpolation] 第 219 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Calling model {model_name}")
  ```

**14. [PL-logging-fstring-interpolation] 第 227 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**15. [PL-unspecified-encoding] 第 256 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file, "+a") as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, "+a", encoding="utf-8") as f:
  ```

**16. [PL-logging-fstring-interpolation] 第 259 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Wrote output to {output_file}")
  ```

#### 📄 `run_llama.py`

**1. [AST002] 第 61 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if min_len is not None:
  ```
- **修复方案**:
  ```python
  if min_len == not None:
  ```

**2. [AST002] 第 63 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if max_len is not None:
  ```
- **修复方案**:
  ```python
  if max_len == not None:
  ```

**3. [AST002] 第 65 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**4. [AST002] 第 65 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**5. [AST002] 第 71 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if peft_path is not None and "checkpoint" in Path(peft_path).name:
  ```
- **修复方案**:
  ```python
  if peft_path == not None and "checkpoint" in Path(peft_path).name:
  ```

**6. [AST002] 第 73 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif peft_path is not None:
  ```
- **修复方案**:
  ```python
  elif peft_path == not None:
  ```

**7. [AST002] 第 143 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if peft_path is None:
  ```
- **修复方案**:
  ```python
  if peft_path == None:
  ```

**8. [AST002] 第 200 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if peft_path is not None:
  ```
- **修复方案**:
  ```python
  if peft_path == not None:
  ```

**9. [AST002] 第 216 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if min_len is not None and max_len is None:
  ```
- **修复方案**:
  ```python
  if min_len == not None and max_len == None:
  ```

**10. [AST002] 第 216 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if min_len is not None and max_len is None:
  ```
- **修复方案**:
  ```python
  if min_len == not None and max_len == None:
  ```

**11. [AST002] 第 218 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif min_len is None and max_len is not None:
  ```
- **修复方案**:
  ```python
  elif min_len == None and max_len == not None:
  ```

**12. [AST002] 第 218 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif min_len is None and max_len is not None:
  ```
- **修复方案**:
  ```python
  elif min_len == None and max_len == not None:
  ```

**13. [AST002] 第 220 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif min_len is not None and max_len is not None:
  ```
- **修复方案**:
  ```python
  elif min_len == not None and max_len == not None:
  ```

**14. [AST002] 第 220 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  elif min_len is not None and max_len is not None:
  ```
- **修复方案**:
  ```python
  elif min_len == not None and max_len == not None:
  ```

**15. [AST002] 第 222 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if filter_func is not None:
  ```
- **修复方案**:
  ```python
  if filter_func == not None:
  ```

**16. [AST002] 第 228 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**17. [AST002] 第 228 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**18. [AST002] 第 235 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**19. [AST002] 第 235 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == not None:
  ```

**20. [AST002] 第 322 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  model_name_or_path += f"__{peft_path}" if peft_path is not None else ""
  ```
- **修复方案**:
  ```python
  model_name_or_path += f"__{peft_path}" if peft_path == not None else ""
  ```

**21. [AST002] 第 345 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if match is None:
  ```
- **修复方案**:
  ```python
  if match == None:
  ```

**22. [AST002] 第 373 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == None:
  ```

**23. [AST002] 第 373 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is not None and num_shards is None:
  ```
- **修复方案**:
  ```python
  if shard_id == not None and num_shards == None:
  ```

**24. [AST002] 第 375 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == None and num_shards == not None:
  ```

**25. [AST002] 第 375 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if shard_id is None and num_shards is not None:
  ```
- **修复方案**:
  ```python
  if shard_id == None and num_shards == not None:
  ```

**26. [AST002] 第 378 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if peft_path is not None:
  ```
- **修复方案**:
  ```python
  if peft_path == not None:
  ```

**27. [PL-unspecified-encoding] 第 25 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  DEVICE_MAPS = json.load(open(Path(__file__).parent / "codellama_device_maps.json"))
  ```
- **修复方案**:
  ```python
  DEVICE_MAPS = json.load(open(Path(__file__, encoding="utf-8").parent / "codellama_device_maps.json", encoding="utf-8"), encoding="utf-8")
  ```

**28. [PL-logging-fstring-interpolation] 第 119 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Loading base model from {model_name_or_path}")
  ```

**29. [PL-logging-fstring-interpolation] 第 127 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Using max memory {max_memory}")
  ```

**30. [PL-logging-fstring-interpolation] 第 136 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Using device_map {device_map}")
  ```

**31. [PL-logging-fstring-interpolation] 第 146 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Loading PEFT adapters from {peft_path}")
  ```

**32. [PL-logging-fstring-interpolation] 第 158 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Loading tokenizer {model_name_or_path}")
  ```

**33. [PL-logging-fstring-interpolation] 第 193 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Loading dataset from {dataset_path}")
  ```

**34. [PL-logging-fstring-interpolation] 第 236 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**35. [PL-logging-fstring-interpolation] 第 240 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(
  ```

**36. [PL-unused-variable] 第 201 行**
- **问题**: Unused variable 'model_nickname'
- **原代码**:
  ```python
  model_nickname = "__".join(peft_path.split("/")[-2:])
  ```

**37. [PL-broad-exception-caught] 第 330 行**
- **问题**: Catching too general exception Exception
- **原代码**:
  ```python
  except Exception as e:
  ```

**38. [PL-logging-fstring-interpolation] 第 300 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Processing {input_ids.shape[-1]} tokens")
  ```

**39. [PL-raise-missing-from] 第 335 行**
- **问题**: Consider explicitly re-raising using 'raise ValueError('too many failures') from e'
- **原代码**:
  ```python
  raise ValueError("too many failures")
  ```

**40. [PL-logging-fstring-interpolation] 第 351 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Loading existing ids from existing {filename}")
  ```

**41. [PL-unspecified-encoding] 第 352 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(filename) as f:
  ```
- **修复方案**:
  ```python
  with open(filename, encoding="utf-8") as f:
  ```

**42. [PL-logging-fstring-interpolation] 第 356 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.info(f"Found {len(existing_ids)} existing ids")
  ```

**43. [PL-logging-fstring-interpolation] 第 381 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(
  ```

**44. [PL-logging-fstring-interpolation] 第 397 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"output_file: {output_file}")
  ```

**45. [PL-unspecified-encoding] 第 413 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(output_file, "a") as f:
  ```
- **修复方案**:
  ```python
  with open(output_file, "a", encoding="utf-8") as f:
  ```

**46. [B615] 第 159 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in from_pretrained()
- **原代码**:
  ```python
  tokenizer = LlamaTokenizer.from_pretrained(model_name_or_path)
  ```

**47. [B615] 第 195 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dataset = load_dataset(dataset_path, split=split)
  ```

**48. [B615] 第 199 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dataset = load_dataset(dataset_path)[split]
  ```

#### 📄 `rust.py`

**1. [PL-unused-argument] 第 7 行**
- **问题**: Unused argument 'test_spec'
- **原代码**:
  ```python
  def parse_log_cargo(log: str, test_spec: TestSpec) -> dict[str, str]:
  ```

#### 📄 `test_cli.py`

**1. [PL-subprocess-run-check] 第 6 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**2. [PL-subprocess-run-check] 第 26 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

#### 📄 `test_collect_cli.py`

**1. [PL-subprocess-run-check] 第 7 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**2. [PL-subprocess-run-check] 第 24 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**3. [PL-subprocess-run-check] 第 39 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**4. [PL-subprocess-run-check] 第 57 行**
- **问题**: 'subprocess.run' used without explicitly defining the value for 'check'.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

#### 📄 `test_spec.py`

**1. [AST002] 第 115 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  return self.namespace is not None
  ```
- **修复方案**:
  ```python
  return self.namespace == not None
  ```

**2. [AST002] 第 184 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert base_image_tag is not None, "base_image_tag cannot be None"
  ```
- **修复方案**:
  ```python
  assert base_image_tag == not None, "base_image_tag cannot be None"
  ```

**3. [AST002] 第 185 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert env_image_tag is not None, "env_image_tag cannot be None"
  ```
- **修复方案**:
  ```python
  assert env_image_tag == not None, "env_image_tag cannot be None"
  ```

**4. [AST002] 第 186 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  assert instance_image_tag is not None, "instance_image_tag cannot be None"
  ```
- **修复方案**:
  ```python
  assert instance_image_tag == not None, "instance_image_tag cannot be None"
  ```

**5. [PL-unused-variable] 第 191 行**
- **问题**: Unused variable 'problem_statement'
- **原代码**:
  ```python
  problem_statement = instance.get("problem_statement")
  ```

**6. [PL-unused-variable] 第 192 行**
- **问题**: Unused variable 'hints_text'
- **原代码**:
  ```python
  hints_text = instance.get("hints_text")  # Unused
  ```

#### 📄 `tokenize_dataset.py`

**1. [AST002] 第 39 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["text"] is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if instance["text"] == None or instance["patch"] == None:
  ```

**2. [AST002] 第 39 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["text"] is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if instance["text"] == None or instance["patch"] == None:
  ```

**3. [AST002] 第 43 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if text_inputs is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if text_inputs == None or instance["patch"] == None:
  ```

**4. [AST002] 第 43 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if text_inputs is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if text_inputs == None or instance["patch"] == None:
  ```

**5. [AST002] 第 76 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["text"] is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if instance["text"] == None or instance["patch"] == None:
  ```

**6. [AST002] 第 76 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if instance["text"] is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if instance["text"] == None or instance["patch"] == None:
  ```

**7. [AST002] 第 80 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if text_inputs is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if text_inputs == None or instance["patch"] == None:
  ```

**8. [AST002] 第 80 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if text_inputs is None or instance["patch"] is None:
  ```
- **修复方案**:
  ```python
  if text_inputs == None or instance["patch"] == None:
  ```

**9. [AST002] 第 116 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if push_to_hub_user is not None:
  ```
- **修复方案**:
  ```python
  if push_to_hub_user == not None:
  ```

**10. [AST002] 第 118 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if hub_token is None:
  ```
- **修复方案**:
  ```python
  if hub_token == None:
  ```

**11. [AST002] 第 123 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if tokenizer_name is not None:
  ```
- **修复方案**:
  ```python
  if tokenizer_name == not None:
  ```

**12. [AST002] 第 201 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if push_to_hub_user is not None:
  ```
- **修复方案**:
  ```python
  if push_to_hub_user == not None:
  ```

**13. [PL-unused-argument] 第 74 行**
- **问题**: Unused argument 'tokenizer_name'
- **原代码**:
  ```python
  def extract_test_fields(instance, tokenizer_name, tokenizer, tokenizer_func, eos_token):
  ```

**14. [PL-logging-fstring-interpolation] 第 171 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"Split {split} not in dataset. Skipping")
  ```

**15. [PL-logging-fstring-interpolation] 第 207 行**
- **问题**: Use lazy % formatting in logging functions
- **原代码**:
  ```python
  logger.warning(f"Saved to {output_file}")
  ```

**16. [B615] 第 33 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in from_pretrained()
- **原代码**:
  ```python
  "llama": (LlamaTokenizer.from_pretrained("togethercomputer/LLaMA-2-7B-32K"), llama),
  ```

**17. [B615] 第 135 行**
- **问题**: Unsafe Hugging Face Hub download without revision pinning in load_dataset()
- **原代码**:
  ```python
  dataset = load_dataset(dataset_name_or_path)
  ```

#### 📄 `utils.py`

**1. [AST002] 第 149 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if num_pages is not None and page >= num_pages:
  ```
- **修复方案**:
  ```python
  if num_pages == not None and page >= num_pages:
  ```

**2. [AST002] 第 257 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if issue is None:
  ```
- **修复方案**:
  ```python
  if issue == None:
  ```

**3. [AST002] 第 387 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if comment_resp is None or timestamp_resp is None:
  ```
- **修复方案**:
  ```python
  if comment_resp == None or timestamp_resp == None:
  ```

**4. [AST002] 第 387 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if comment_resp is None or timestamp_resp is None:
  ```
- **修复方案**:
  ```python
  if comment_resp == None or timestamp_resp == None:
  ```

**5. [AST002] 第 66 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if model_patch is None:
  ```
- **修复方案**:
  ```python
  if model_patch == None:
  ```

**6. [AST002] 第 118 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if response is None:
  ```
- **修复方案**:
  ```python
  if response == None:
  ```

**7. [AST002] 第 142 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if test_phrases is None:
  ```
- **修复方案**:
  ```python
  if test_phrases == None:
  ```

**8. [AST002] 第 187 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if token is None:
  ```
- **修复方案**:
  ```python
  if token == None:
  ```

**9. [AST002] 第 190 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if root_dir is None:
  ```
- **修复方案**:
  ```python
  if root_dir == None:
  ```

**10. [AST002] 第 208 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if self.tempdir is not None:
  ```
- **修复方案**:
  ```python
  if self.tempdir == not None:
  ```

**11. [AST002] 第 282 行**
- **问题**: 疑似使用 'is' 进行值比较，建议使用 '==' （is 仅用于 None/True/False）。
- **原代码**:
  ```python
  if encoding is None:
  ```
- **修复方案**:
  ```python
  if encoding == None:
  ```

**12. [PL-unspecified-encoding] 第 15 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(instance_path) as f:
  ```
- **修复方案**:
  ```python
  with open(instance_path, encoding="utf-8") as f:
  ```

**13. [PL-unspecified-encoding] 第 20 行**
- **问题**: Using open without explicitly specifying an encoding
- **原代码**:
  ```python
  with open(instance_path) as f:
  ```
- **修复方案**:
  ```python
  with open(instance_path, encoding="utf-8") as f:
  ```

### 🟢 LOW 级别 (199 个)

#### 📄 `__init__.py`

**1. [PY050] 第 5 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.c import *
  ```

**2. [PY050] 第 6 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.go import *
  ```

**3. [PY050] 第 7 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.java import *
  ```

**4. [PY050] 第 8 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.javascript import *
  ```

**5. [PY050] 第 9 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.php import *
  ```

**6. [PY050] 第 10 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.python import *
  ```

**7. [PY050] 第 11 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.ruby import *
  ```

**8. [PY050] 第 12 行**
- **问题**: 使用 from X import * 可能导致命名冲突。
- **原代码**:
  ```python
  from swebench.harness.constants.rust import *
  ```

#### 📄 `bm25_retrieval.py`

**1. [PY011] 第 58 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PY011] 第 96 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PY010] 第 136 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```

**4. [PY011] 第 138 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**5. [PY011] 第 332 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**6. [PY011] 第 343 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception:
  ```

**7. [PY010] 第 409 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```

**8. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**9. [RUFF-B904] 第 263 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise KeyboardInterrupt
  ```

**10. [B404] 第 7 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**11. [B112] 第 136 行**
- **问题**: Try, Except, Continue detected.
- **原代码**:
  ```python
  except:
  ```

**12. [B603] 第 254 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  proc = subprocess.Popen(
  ```

**13. [B607] 第 486 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  python = subprocess.run("which python", shell=True, capture_output=True)
  ```

**14. [B602] 第 486 行**
- **问题**: subprocess call with shell=True seems safe, but may be changed in the future, consider rewriting without shell
- **原代码**:
  ```python
  python = subprocess.run("which python", shell=True, capture_output=True)
  ```

#### 📄 `build_dataset.py`

**1. [RUFF-I001] 第 3 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import argparse
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `build_dataset_ft.py`

**1. [RUFF-I001] 第 3 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import argparse
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `c.py`

**1. [B405] 第 2 行**
- **问题**: Using xml.etree.ElementTree to parse untrusted XML data is known to be vulnerable to XML attacks. Replace xml.etree.ElementTree with the equivalent defusedxml package, or make sure defusedxml.defuse_stdlib() is called.
- **原代码**:
  ```python
  import xml.etree.ElementTree as ET
  ```

#### 📄 `call_make_repo.py`

**1. [B404] 第 3 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

#### 📄 `create_instance.py`

**1. [PY011] 第 484 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [B101] 第 309 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert retrieval_results_path.exists(), (
  ```

**4. [B101] 第 362 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert progress_file is not None, "progress_file is required"
  ```

**5. [B101] 第 384 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert tokenizer_name is not None, (
  ```

#### 📄 `create_scripts.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from swebench.harness.constants import MAP_REPO_TO_EXT
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `create_text_dataset.py`

**1. [PY203] 第 251 行**
- **问题**: list.remove() 要求参数为列表中的元素，而非索引。
- **原代码**:
  ```python
  os.remove(progress_file)
  ```

**2. [RUFF-I001] 第 7 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [B101] 第 77 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert hub_token is not None, (
  ```

**4. [B101] 第 80 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert output_dir is None, "Cannot provide output_dir if pushing to the Hub"
  ```

**5. [B101] 第 82 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert tokenizer_name is not None
  ```

**6. [B101] 第 86 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert file_source not in {"all", "oracle"}, (
  ```

**7. [B101] 第 89 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert tokenizer_name is not None, (
  ```

**8. [B101] 第 93 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert file_source not in {"all", "oracle"}, (
  ```

#### 📄 `criteria.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import re
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `default.py`

**1. [RUFF-I001] 第 3 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import re
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [RUFF-B904] 第 117 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise ExecutionTimeoutError(
  ```

**3. [RUFF-B904] 第 121 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise ExecutionTimeoutError(self.render_template(self.config.timeout_template, action=action, output=""))
  ```

**4. [B404] 第 4 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

#### 📄 `delete_gh_workflows.py`

**1. [B404] 第 5 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**2. [B607] 第 16 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  branches_command = subprocess.run(
  ```

**3. [B603] 第 16 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  branches_command = subprocess.run(
  ```

**4. [B607] 第 21 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(
  ```

**5. [B603] 第 21 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(
  ```

**6. [B607] 第 33 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(["git", "checkout", branch])
  ```

**7. [B603] 第 33 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(["git", "checkout", branch])
  ```

**8. [B607] 第 39 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(["rm", "-rf", workflows_path])
  ```

**9. [B603] 第 39 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(["rm", "-rf", workflows_path])
  ```

**10. [B607] 第 40 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(["git", "add", "-A"])
  ```

**11. [B603] 第 40 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(["git", "add", "-A"])
  ```

**12. [B607] 第 41 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(["git", "commit", "-m", "Remove .github/workflows folder"])
  ```

**13. [B603] 第 41 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(["git", "commit", "-m", "Remove .github/workflows folder"])
  ```

**14. [B607] 第 42 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(["git", "push"])
  ```

**15. [B603] 第 42 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(["git", "push"])
  ```

**16. [B607] 第 47 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(["rm", "-rf", "temp_repo"])
  ```

**17. [B603] 第 47 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(["rm", "-rf", "temp_repo"])
  ```

#### 📄 `distributed_attention.py`

**1. [RUFF-I001] 第 6 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import torch
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `docker_build.py`

**1. [PY011] 第 155 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PY011] 第 502 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PY011] 第 527 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**4. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from __future__ import annotations
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**5. [RUFF-B010] 第 58 行**
- **问题**: Do not call `setattr` with a constant attribute value. It is not any safer than normal property access.
- **原代码**:
  ```python
  setattr(logger, "log_file", log_file)
  ```

**6. [RUFF-B904] 第 248 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise Exception(
  ```

**7. [RUFF-B904] 第 503 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise Exception(
  ```

#### 📄 `docker_utils.py`

**1. [PY011] 第 94 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PY011] 第 136 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PY011] 第 153 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e2:
  ```

**4. [PY011] 第 166 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**5. [PY011] 第 198 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**6. [PY011] 第 289 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**7. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from __future__ import annotations
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `eval_retrieval.py`

**1. [PY010] 第 21 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```

**2. [RUFF-I001] 第 5 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import re
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `get_tasks_pipeline.py`

**1. [PY011] 第 89 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [RUFF-I001] 第 5 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import argparse
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [RUFF-B905] 第 135 行**
- **问题**: `zip()` without an explicit `strict=` parameter
- **原代码**:
  ```python
  for repos, token in zip(data_task_lists, tokens)
  ```

#### 📄 `get_top_pypi.py`

**1. [PY010] 第 74 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```

**2. [RUFF-I001] 第 3 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import os
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [B110] 第 74 行**
- **问题**: Try, Except, Pass detected.
- **原代码**:
  ```python
  except:
  ```

#### 📄 `get_versions.py`

**1. [PY203] 第 237 行**
- **问题**: list.remove() 要求参数为列表中的元素，而非索引。
- **原代码**:
  ```python
  os.remove(task_with_version_path)
  ```

**2. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import argparse
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [B404] 第 8 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**4. [B607] 第 158 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(
  ```

**5. [B602] 第 159 行**
- **问题**: subprocess call with shell=True seems safe, but may be changed in the future, consider rewriting without shell
- **原代码**:
  ```python
  "git restore .", check=True, shell=True, stdout=subprocess.DEVNULL
  ```

**6. [B607] 第 161 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(
  ```

**7. [B602] 第 162 行**
- **问题**: subprocess call with shell=True seems safe, but may be changed in the future, consider rewriting without shell
- **原代码**:
  ```python
  "git reset HEAD .", check=True, shell=True, stdout=subprocess.DEVNULL
  ```

**8. [B607] 第 164 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.run(
  ```

**9. [B602] 第 165 行**
- **问题**: subprocess call with shell=True seems safe, but may be changed in the future, consider rewriting without shell
- **原代码**:
  ```python
  "git clean -fd", shell=True, check=True, stdout=subprocess.DEVNULL
  ```

**10. [B101] 第 293 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert len(data_tasks) == merge_results(
  ```

**11. [B101] 第 308 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert any([x == args.retrieval_method for x in ["build", "mix"]])
  ```

**12. [B101] 第 309 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert all([x in args for x in ["testbed", "path_conda", "conda_env"]])
  ```

**13. [B101] 第 365 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert (
  ```

**14. [B101] 第 371 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert len(data_tasks) == merge_results(
  ```

#### 📄 `get_versions_astropy.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `get_versions_matplotlib.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `get_versions_pvlib-python.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `get_versions_pydicom.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import datetime
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `get_versions_sqlfluff.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [B105] 第 10 行**
- **问题**: Possible hardcoded password: '<your GitHub token>'
- **原代码**:
  ```python
  GITHUB_TOKEN = "<your GitHub token>"
  ```

#### 📄 `get_versions_xarray.py`

**1. [PY010] 第 40 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```

**2. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [B112] 第 40 行**
- **问题**: Try, Except, Continue detected.
- **原代码**:
  ```python
  except:
  ```

#### 📄 `go.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import re
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `grading.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from typing import Any
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `java.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import re
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `javascript.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `make_lite.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from criteria import (
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `modeling_flash_llama.py`

**1. [RUFF-I001] 第 21 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from typing import Optional, Union, Any
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [RUFF-B904] 第 49 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise ImportError(
  ```

**3. [RUFF-B011] 第 251 行**
- **问题**: Do not `assert False` (`python -O` removes these calls), raise `AssertionError()`
- **原代码**:
  ```python
  assert False
  ```

**4. [B101] 第 251 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert False
  ```

**5. [B101] 第 329 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert scaling_type == "linear"
  ```

**6. [B101] 第 413 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert output_attentions is False
  ```

**7. [B101] 第 426 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert attention_mask is not None
  ```

#### 📄 `php.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import re
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `prepare_images.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import docker
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `print_pulls.py`

**1. [RUFF-I001] 第 5 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from __future__ import annotations
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [RUFF-B010] 第 44 行**
- **问题**: Do not call `setattr` with a constant attribute value. It is not any safer than normal property access.
- **原代码**:
  ```python
  setattr(pull, "resolved_issues", repo.extract_resolved_issues(pull))
  ```

**3. [RUFF-B010] 第 77 行**
- **问题**: Do not call `setattr` with a constant attribute value. It is not any safer than normal property access.
- **原代码**:
  ```python
  setattr(pull, "resolved_issues", repo.extract_resolved_issues(pull))
  ```

#### 📄 `python.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import os
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `remove_containers.py`

**1. [PY011] 第 35 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import docker
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `remove_envs.py`

**1. [RUFF-I001] 第 3 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import argparse
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [RUFF-B905] 第 92 行**
- **问题**: `zip()` without an explicit `strict=` parameter
- **原代码**:
  ```python
  remove_environment, zip(conda_envs_names, [args.prefix] * len(conda_envs_names))
  ```

**3. [B404] 第 5 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**4. [B603] 第 40 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.run(command.split(" "))
  ```

**5. [B603] 第 52 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  conda_create_output = subprocess.run(
  ```

**6. [B603] 第 79 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  conda_envs = subprocess.run(
  ```

#### 📄 `reporting.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import docker
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `run_api.py`

**1. [PY011] 第 274 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PY011] 第 315 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PY011] 第 389 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**4. [RUFF-I001] 第 7 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `run_evaluation.py`

**1. [PY010] 第 137 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```

**2. [PY011] 第 257 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from __future__ import annotations
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**4. [RUFF-I001] 第 12 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from argparse import ArgumentParser, ArgumentDefaultsHelpFormatter
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**5. [RUFF-B012] 第 270 行**
- **问题**: `return` inside `finally` blocks cause exceptions to be silenced
- **原代码**:
  ```python
  return {
  ```

**6. [B110] 第 137 行**
- **问题**: Try, Except, Pass detected.
- **原代码**:
  ```python
  except:
  ```

**7. [B101] 第 504 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert len(run_id) > 0, "Run ID must be provided"
  ```

#### 📄 `run_evaluation_modal.py`

**1. [PY011] 第 98 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PY011] 第 134 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PY011] 第 150 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception:
  ```

**4. [PY011] 第 154 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception:
  ```

**5. [PY011] 第 258 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**6. [PY011] 第 376 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**7. [PY011] 第 458 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception:
  ```

**8. [RUFF-I001] 第 3 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from __future__ import annotations
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**9. [RUFF-I001] 第 31 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  from swebench.harness.constants import (
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**10. [B110] 第 150 行**
- **问题**: Try, Except, Pass detected.
- **原代码**:
  ```python
  except Exception:
  ```

**11. [B110] 第 154 行**
- **问题**: Try, Except, Pass detected.
- **原代码**:
  ```python
  except Exception:
  ```

#### 📄 `run_evaluation_modal_entrypoint.py`

**1. [RUFF-I001] 第 8 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import asyncio
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `run_live.py`

**1. [RUFF-I001] 第 10 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [RUFF-B905] 第 198 行**
- **问题**: `zip()` without an explicit `strict=` parameter
- **原代码**:
  ```python
  for issue, commit in tqdm(zip(issue_url, base_commit), total=len(issue_url)):
  ```

**3. [B404] 第 11 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**4. [B607] 第 113 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo_dir)
  ```

**5. [B603] 第 113 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo_dir)
  ```

**6. [B607] 第 196 行**
- **问题**: Starting a process with a partial executable path
- **原代码**:
  ```python
  python = subprocess.check_output(["which", "python"]).decode("utf-8").strip()
  ```

**7. [B603] 第 196 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  python = subprocess.check_output(["which", "python"]).decode("utf-8").strip()
  ```

#### 📄 `run_llama.py`

**1. [PY011] 第 330 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import json
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**3. [RUFF-B904] 第 335 行**
- **问题**: Within an `except` clause, raise exceptions with `raise ... from err` or `raise ... from None` to distinguish them from errors in exception handling
- **原代码**:
  ```python
  raise ValueError("too many failures")
  ```

#### 📄 `test_cli.py`

**1. [B404] 第 1 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**2. [B603] 第 6 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**3. [B101] 第 9 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert result.returncode == 0
  ```

**4. [B603] 第 26 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**5. [B101] 第 29 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert result.returncode == 0
  ```

#### 📄 `test_collect_cli.py`

**1. [B404] 第 2 行**
- **问题**: Consider possible security implications associated with the subprocess module.
- **原代码**:
  ```python
  import subprocess
  ```

**2. [B603] 第 7 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**3. [B101] 第 10 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert result.returncode == 0
  ```

**4. [B603] 第 24 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**5. [B101] 第 27 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert result.returncode == 0
  ```

**6. [B603] 第 39 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**7. [B101] 第 42 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert result.returncode == 0
  ```

**8. [B603] 第 57 行**
- **问题**: subprocess call - check for execution of untrusted input.
- **原代码**:
  ```python
  result = subprocess.run(cmd, capture_output=True)
  ```

**9. [B101] 第 60 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert result.returncode == 0
  ```

**10. [B101] 第 62 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert len(data["resolved_issues"]) == 2
  ```

**11. [B101] 第 63 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert sorted(data["resolved_issues"]) == ["26194", "26230"]
  ```

#### 📄 `test_evaluation.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import collections
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [B101] 第 29 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert output_path.is_file()
  ```

**3. [B101] 第 31 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert report["schema_version"] == 2
  ```

#### 📄 `test_harness_utils.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import unittest
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

#### 📄 `test_spec.py`

**1. [RUFF-I001] 第 1 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import hashlib
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [B101] 第 184 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert base_image_tag is not None, "base_image_tag cannot be None"
  ```

**3. [B101] 第 185 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert env_image_tag is not None, "env_image_tag cannot be None"
  ```

**4. [B101] 第 186 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert instance_image_tag is not None, "instance_image_tag cannot be None"
  ```

#### 📄 `tokenize_dataset.py`

**1. [RUFF-I001] 第 5 行**
- **问题**: Import block is un-sorted or un-formatted
- **原代码**:
  ```python
  import os
  ```
- **修复方案**:
  ```python
  # 使用 'ruff check --fixers' 或 'isort' 自动排序导入
  ```

**2. [B101] 第 55 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert idx <= 2, (
  ```

**3. [B101] 第 64 行**
- **问题**: Use of assert detected. The enclosed code will be removed when compiling to optimised byte code.
- **原代码**:
  ```python
  assert len(inputs) == len(labels)
  ```

#### 📄 `utils.py`

**1. [PY011] 第 152 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**2. [PY011] 第 104 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception as e:
  ```

**3. [PY011] 第 127 行**
- **问题**: 过于宽泛的异常捕获：Exception。
- **原代码**:
  ```python
  except Exception:
  ```

**4. [PY010] 第 330 行**
- **问题**: 使用裸 except，建议捕获具体异常类型。
- **原代码**:
  ```python
  except:
  ```
